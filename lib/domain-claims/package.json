{
  "name": "@szl-holdings/lib-domain-claims",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "description": "Shared domain-claims infrastructure: ClaimValue type, claim resolver factory, and metricDisplay helper. All artifacts import from here instead of defining their own copies.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "biome lint ./src",
    "lint:ci": "biome lint ./src",
    "format": "biome format --write ./src",
    "build": "tsc --build",
    "typecheck": "tsc -b tsconfig.json",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@szl-holdings/config": "workspace:*"
  }
}
