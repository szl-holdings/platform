{
  "name": "@szl-holdings/api-spec",
  "version": "0.0.0",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/szl-holdings/platform.git",
    "directory": "lib/api-spec"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "scripts": {
    "codegen": "node -e \"const fs=require('node:fs');const ok=fs.existsSync('../api-client-react/src/generated/api.ts')&&fs.existsSync('../api-zod/src/generated/api.ts');if(ok)console.log('codegen artifacts present, skipping orval');process.exit(ok?0:1)\" || orval --config ./orval.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "orval": "^8.5.2",
    "prettier": "^3.8.3"
  }
}
