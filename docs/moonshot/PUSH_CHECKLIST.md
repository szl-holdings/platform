# GitHub Push Checklist — Moonshot Phases 1–8

**Date:** 2026-04-25  
**Branch:** `master`  
**Target repo:** `szl-holdings/platform` (GitHub)  
**Prepared by:** Platform Engineering — Moonshot Phase 8

---

## Environment Note

All git remotes currently configured in this Replit workspace are internal `subrepl-*` remotes. **There is no `origin` or GitHub remote pre-configured.** The GitHub remote must be added manually before pushing. The steps below include that setup.

```bash
# Confirm no GitHub remote exists yet:
git remote -v | grep github
# Expected: no output (confirming no GitHub remote is present)
```

---

## Pre-Push Checklist

Before running the push commands, confirm every item is checked:

- [ ] `node scripts/audit/validate-source-of-truth.js` exits 0
- [ ] `pnpm test:unit` — 227/227 tests pass
- [ ] `pnpm brand:check` — 0 violations
- [ ] `pnpm audit:mocks` — passes
- [ ] `pnpm audit:deps` — passes
- [ ] `pnpm audit:design-system` — passes
- [ ] `pnpm audit:copy` — passes
- [ ] `pnpm check-boundaries` — passes
- [ ] `RELEASE_READINESS_SCORECARD.md` reflects current state
- [ ] `PR_DRAFT.md` reviewed and accurate
- [ ] No secrets or credential strings in the diff (`git diff --stat HEAD~1 HEAD`)
- [ ] `gitleaks detect --source .` passes (or is configured in pre-commit)

---

## Step 1 — Add the GitHub Remote

Choose SSH (preferred) or HTTPS:

```bash
# SSH (requires SSH key added to your GitHub account)
git remote add github git@github.com:szl-holdings/platform.git

# OR HTTPS (will prompt for GitHub username + personal access token)
git remote add github https://github.com/szl-holdings/platform.git

# Verify the remote was added
git remote -v | grep github
# Expected:
#   github  git@github.com:szl-holdings/platform.git (fetch)
#   github  git@github.com:szl-holdings/platform.git (push)
```

---

## Step 2 — Verify Branch and Status

```bash
git branch --show-current
# Expected: master

git status
# Expected: nothing to commit, working tree clean
```

---

## Step 3 — Push

```bash
git push github master
```

---

## Step 4 — Confirm CI Triggers

Navigate to: `https://github.com/szl-holdings/platform/actions`

Verify the following workflows start within 60 seconds:
- CI Gate
- E2E Gate
- Lighthouse Gate
- dependency-review
- CodeQL (analyze)

---

## Step 5 — (Optional) Tag a Release

```bash
git tag -a v1.1.0-moonshot -m "Moonshot Phases 1–8: governed agentic runtime, frontend overhaul, ops hardening"
git push github v1.1.0-moonshot
```

---

## Post-Push Actions

1. **Open PR** — use `PR_DRAFT.md` as the PR body
2. **Confirm CI passes** — all 5 required status checks must be green before merge
3. **Request code-owner review** — branch protection requires review
4. **Check Dependabot** — after merge, any security updates should be triaged within 48 hours
5. **Update `CHANGELOG.md`** — move `## [Unreleased]` items to a versioned section if tagging a release

---

## Commit Group Reference (for reviewers inspecting history)

| Stream | Description | Phase |
|--------|-------------|-------|
| `audit/reconciliation` | SOT v1.3.0, brand corrections, stale cleanup | Phase 1 |
| `agent/runtime` | Alloy coordinator, specialist scaffold, KORA lane | Phase 2 |
| `speech/document` | Speech + Document specialist production wiring | Phase 3 |
| `retrieval/memory` | Vector search hooks, AEF evidence ledger | Phase 4 |
| `forecast/anomaly` | ForecastingSpecialist + AnomalySpecialist fabric wiring | Phase 5 |
| `frontend/overhaul` | De-gamification, backbone UI consumption, visual QA | Phase 6 |
| `ops/release` | Secrets separation, tenant isolation, eval IaC | Phase 7 |
| `docs/release` | EXECUTIVE_AUDIT_SUMMARY finalized, RELEASE_READINESS_SCORECARD, PR_DRAFT, MODEL_BACKBONE_BLUEPRINT corrections | Phase 8 |

---

*Generated by Moonshot Phase 8 — GitHub Push Prep, 2026-04-25.*
