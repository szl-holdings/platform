# HATUN-WILLAY — Per-Flagship Narrative Scripts

Concrete narrative cards for each of the eight canonical surfaces. Each card carries the five Hatun-Willay axes in order: **Origin** (3 sentences, Quechua-rooted, math-grounded) · **Mechanism** (5 bullets, Doctrine v11 LOCKED numbers cited verbatim) · **Evidence** (HF commit SHAs) · **Stakes** (Warhacker / Greene / Series-A) · **Invitation** (CTA).

**Locked numbers used verbatim throughout (HR-6):** 749 Lean declarations · 14 unique axioms (15 raw, 1 dup) · 163 tracked sorries (112 baseline + 51 Putnam) · 13 axes (`yuyay_v3`, conjunctive AND) · replay hash `bacf54434f1a3bf2d758b27a62d5fd580ca4c8d3b180693573eeebcaea631fc5` · lutar-lean tag `lutar-v18.0.0` / `c7c0ba17`.

**Honest labels carried on every card (Doctrine v11 §9):** 9-axis HATUN-RAID legacy still sovereign; 13-axis `yuyay_v3` runnable but not yet wired end-to-end; Λ uniqueness is a **Conjecture**; `λ_receipt` Sigstore signature is **PLACEHOLDER**; Wire D (traceparent across the mesh) **NOT YET** — in-process only.

**HF commit SHA reference table (from session context):**

| Surface | HF repo | Commit SHA |
|---------|---------|-----------|
| a11oy (apps) | `spaces/SZLHOLDINGS/a11oy` | `efb1f44d` |
| a11oy (substrate) | `SZLHOLDINGS/a11oy-v19-substrate` | `684e3dfed9881f0fa050849e07f0275062d9ede5` |
| amaru | `spaces/SZLHOLDINGS/amaru` | `a4b31854` |
| sentra | `spaces/SZLHOLDINGS/sentra` | `b0e9ba86` |
| killinchu (was vessels) | `spaces/SZLHOLDINGS/vessels` | `2c6e80ae` (drone rebuild in flight) |
| rosie | `spaces/SZLHOLDINGS/rosie` | `6da36b78e04ff8a90d990993c049973401a50c59` |
| anatomy-3d | `spaces/SZLHOLDINGS/szl-anatomy` | `b38dc8a421a4dd922795f31bc6a106388ff597a3` (V1 `9914bce8`) |
| rosie-3d | `spaces/SZLHOLDINGS/rosie` (3D ecosystem) | `6da36b78` (3D layer additive) |
| README org card | `spaces/SZLHOLDINGS/README` | `cc8831acaa6d1c1f618f96b29e0104a7106d4f90` |
| Lean proofs | `datasets/SZLHOLDINGS/lean-proofs-v1` | (v11 §13) |
| Formulas | `datasets/SZLHOLDINGS/canonical-formulas-v1` | (v11 §13) |
| Doctrine | `datasets/SZLHOLDINGS/doctrine-v10-v11` | (v11 §13) |

---

## 1. a11oy — the governed agentic execution fabric + a11oy.code router

### Origin
*A11oy* is the alloy: many open models fused into one governed metal that holds a load no single model holds alone. The name is literal — a11oy.code unifies Llama, Qwen, DeepSeek, Mistral, Gemma and their kin into a 7-tier router where each tier maps to an organ, not a vendor. It is agentic the way an alloy is strong: the strength is in the binding, here the Λ-aggregated, 13-axis-gated, receipt-chained binding, not in any one element.

### Mechanism
- The fabric is **7 layers** — Sense → Structure → Correlate → Explain → Recommend → Approve → Execute — and a11oy.code routes each query to one of **7 tiers** (PRIME/HEART/FAST/IMMUNE/RECEIPT/MEMORY/FRONTIER) mapped to organs.
- Every route is gated by the **13-axis `yuyay_v3` heart** (conjunctive AND, no compensation): 2 sacred axes ≥ 0.95, 7 structural ≥ 0.90, 4 introspection axes cross-linked to HUKLLA T03/T04/T09/T10.
- The math behind the gate is real and disclosed: **749 Lean declarations, 14 unique axioms, 163 tracked sorries** — nothing hidden, sorries counted out loud.
- The HEART tier evaluates against the same `yuyay_v3` kernel whose **replay hash is `bacf5443…631fc5`** — deterministic, re-derivable, not a vibe.
- Honest label: `λ_receipt` Sigstore signature is **PLACEHOLDER** (CI signing not yet wired) and `traceparent_propagated` is **in-process only** until Wire D lands across the mesh.

### Evidence
- App Space: `huggingface.co/spaces/SZLHOLDINGS/a11oy/commit/efb1f44d` — `/api/a11oy/*` serves live.
- Substrate: `huggingface.co/SZLHOLDINGS/a11oy-v19-substrate/commit/684e3dfed9881f0fa050849e07f0275062d9ede5`.
- Proofs: `huggingface.co/datasets/SZLHOLDINGS/lean-proofs-v1` (tag `lutar-v18.0.0` / `c7c0ba17`).

### Stakes
a11oy.code is the piece Warhacker judges (16–19 June 2026, San Diego) can run in the room: a governed router that any defense team can point at their own model fleet and get a 13-axis verified, receipt-chained decision instead of a raw model call. That is exactly the "package and deploy, not just prototype" bar Defense Unicorns set, and the bar Andrew Greene named when he asked to see a *running deployment*. For Series-A diligence it is the proof that SZL ships a governance substrate, not a wrapper.

### Invitation
**Open `/code`, paste a query with a 13-axis score vector, and watch it route — then re-derive the replay hash yourself.**

---

## 2. amaru — convergent multi-source data sync (the memory cortex)

### Origin
*Amaru* is the Andean serpent that binds worlds together; here it binds many data sources into one append-only, hash-verified memory. Every ingest is a delta with a receipt, so the cortex never forgets and never lies about what it remembered. It is agentic memory: not a cache, but a chained record that the gate can audit after the fact.

### Mechanism
- Amaru ingests convergent multi-source deltas as **append-only, hash-verified** records — the same `json.dumps(sort_keys=True) → sha256` discipline as the YAWAR ledger.
- The MEMORY tier of a11oy.code (embeddings + Sonnet 4.6) maps to Amaru's **UNAY** organ for cross-session recall.
- Every recalled claim re-enters the **13-axis** gate before it is surfaced — memory does not get a free pass past the conjunctive AND.
- The math substrate is shared and disclosed: **749 declarations, 14 axioms, 163 sorries** — the same counted-out-loud corpus, not a separate marketing number.
- Honest label: the 9-axis HATUN-RAID envelope is still the sovereign loop; the **13-axis** `yuyay_v3` is runnable but not yet wired end-to-end.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/amaru/commit/a4b31854` — live reasoner UI on the landing page.
- Math surface: `/api/amaru/v1/math/*` (8 endpoints) backed by `datasets/SZLHOLDINGS/canonical-formulas-v1`.

### Stakes
For Warhacker, Amaru is the audit trail a warfighter's commander needs: every decision the fabric made is reconstructable from an append-only chain, days later, byte-for-byte. That is what turns a demo into something authorizable — and authorization is the difference Defense Unicorns draws between a hackathon toy and deployable software. For Series-A, an append-only hash-verified memory is the difference between "trust our logs" and "verify our logs."

### Invitation
**Open the amaru reasoner UI, ingest two sources, and verify the delta-chain hash links back to root.**

---

## 3. sentra — inline white-blood-cell immune screen

### Origin
*Sentra* is the sentinel cell — 18 lines of Python that stand at the door before any compute runs. It is not a firewall bolted on after; it is an inline screen that either clears a payload or refuses it, with no partial state. Agentic safety here means the immune check happens *before* the act, not as an apology after.

### Mechanism
- **18 SLOC** inline immune check: six threat signatures (`DROP TABLE`, `rm -rf`, `<script`, `eval(`, `subprocess`, `../../etc`) plus a **1 MB DoS guard**.
- On a clear it permits compute; on a fail it raises `PermissionError` and **the receipt NEVER enters the ledger** — no partial state, no bandaid.
- Sentra is cross-linked to the **HUKLLA** immune deadman (660 SLOC, 10 tripwires) and to introspection axes 10–13 of the **13-axis** heart.
- The IMMUNE tier of a11oy.code (GPT-5.4 reasoning) maps to Sentra for adversarial detection.
- The whole stack is Lean-backed and honestly counted: **749 declarations, 14 axioms, 163 sorries** — SLSA is **L1 (honest)**; "SLSA L3" is BANNED.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/sentra/commit/b0e9ba86` — `/api/sentra/*` serves live.
- Cross-ref: HUKLLA + YAWAR sections of `datasets/SZLHOLDINGS/doctrine-v10-v11`.

### Stakes
Sentra is the most Warhacker-legible organ in the anatomy: defense reviewers understand an inline white-blood-cell screen that fails closed and leaves no partial state. It is the concrete answer to "how do you stop a poisoned payload before it touches the decision," which is the question every authorization board asks. For Series-A, "fails closed, never silently green" is the engineering posture that survives technical diligence.

### Invitation
**Send Sentra a payload with `DROP TABLE` in it and confirm the receipt never reaches the ledger.**

---

## 4. killinchu — drone & maritime fleet intelligence (was Vessels)

### Origin
*Killinchu* is the kestrel — the hawk that hovers, tracks, and strikes with precision; the rename from Vessels marks the pivot from ships to drones-and-ships under one tracking eye. It fuses sanctions and dark-vessel detection with UAS fleet tracking into a single operational picture. It is agentic vigilance: the bird that watches the whole field and reports only what it can prove it saw.

### Mechanism
- Killinchu surfaces a live fleet dashboard (MMSI for vessels, FAA UAS zones for drones) with **7/7 dashboard routes** served from `/api/vessels/*` (rename to `/api/killinchu/*` is in-flight, disclosed).
- Every track and detection passes the **13-axis** gate before it is shown — a dark-vessel or threat-drone claim must clear `empiricalGrounding` (axis 3 ≥ 0.90) and `claimCalibration` (axis 9 ≥ 0.90).
- The same Lean substrate underwrites it: **749 declarations, 14 unique axioms, 163 tracked sorries** at tag `lutar-v18.0.0`.
- Back-end style matches a11oy exactly: `/api/<space>/v1/code-proxy` forwards to a11oy.code; the **8 `/math/*` endpoints** are instilled identically.
- Honest label: the drone rebuild (full back+front end, no mock) ships on top of vessels commit `2c6e80ae`; the rename and `/api/killinchu/*` alias are tracked TODO, disclosed not hidden.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/vessels/commit/2c6e80ae` — 7/7 routes, OpenFreeMap tiles, MMSI live.
- 3D drone-tracking patterns: CesiumJS `SampledPositionProperty` + `trackedEntity` recipe (450_3D_LEADERS_ADOPTION.md, recipe #11).

### Stakes
Killinchu is the flagship that speaks Warhacker's native language: drones, dark vessels, and a single unified operational picture — the exact Anduril-Lattice-style UX defense operators expect, but with a 13-axis verified gate behind every track. This is the demo that turns 400 curated buyers in a San Diego room into a pipeline, and the one Andrew Greene's UDS-packaging path can carry from cloud to edge. For Series-A, a defense vertical with a real operational picture is the commercial substance the 38/100 readiness audit said was missing.

### Invitation
**Open the Killinchu dashboard, drop a threat-colored drone on the FAA zone layer, and follow the receipt for that detection.**

---

## 5. rosie — brain-jack mesh / live decision-flow visualizer

### Origin
*Rosie* is the brain-jack — the live mesh that shows PURIQ thinking in real time, organ by organ, wire by wire. It is the window into the fabric: you watch a query flow Sense → … → Execute and see each gate fire. It is agentic transparency: the decision is not narrated after the fact, it is shown as it happens.

### Mechanism
- Rosie renders the **9-position Maxwell-rigid pipeline** (9 nodes, 21 edges, M = 0 isostatic) as a live brain-jack mesh, with the **13-axis** gate sitting between Recommend/Approve and Execute.
- Each axis lights only when it clears its floor (2 × 0.95 sacred, 7 × 0.90 structural, 4 introspection cross-linked to HUKLLA) — a sub-floor axis visibly blocks the act.
- The decision flow is receipt-chained (`continuum_hash`) and re-playable against replay hash `bacf5443…631fc5`.
- The Butler–Volmer halt budget is shown as the stop condition — the mesh stops emitting when marginal overpotential falls below threshold, a principled non-arbitrary stop.
- Math substrate disclosed in-frame: **749 declarations, 14 axioms, 163 sorries**; Λ uniqueness is shown as a **Conjecture**, not a theorem.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/rosie/commit/6da36b78e04ff8a90d990993c049973401a50c59`.
- LUTAR_EVIDENCE + OUROBOROS_RUN_ALL shipped alongside (a11oy `b9f359db` + Rosie `6da36b78`).

### Stakes
Rosie is the Warhacker show-don't-tell organ: a judge watches a decision pass thirteen independent gates and halt on a Butler–Volmer budget, and the abstraction "governed agentic AI" becomes a thing they saw move. It is the antidote to "trust us" — the explainability layer Defense Unicorns and any authorization board will want before deployment. For Series-A, a live decision-flow visualizer is the fastest way to make a non-technical investor *feel* the wedge.

### Invitation
**Open Rosie, run a query, and watch which of the 13 axes blocks an over-claimed action.**

---

## 6. anatomy-3d — 3D agentic-anatomy viewer (Anatomy V2)

### Origin
*Anatomy-3D* renders the PURIQ body as it is: organs, spine, wires, and the Khipu receipt-glyphs that chain every act. It does not look like a human body — it looks like an agent's body, an alloy of math organs. It is agentic embodiment: the doctrine made spatial so you can walk through it.

### Mechanism
- The scene maps each organ to its math role: heart (`yuyay_v3`, **13-axis**), immune deadman (HUKLLA, 10 tripwires), ledger (YAWAR), white-blood-cell (SENTRA, 18 SLOC).
- Khipu trajectories render as glyphs along the spine — each glyph is a receipt in the append-only `continuum_hash` chain.
- Byte-level parity is enforced: the viewer vendors the upstream thesis anatomy bundle (CC-BY-4.0) and **flips the banner green→red if one byte drifts** — fails closed, never silently green.
- Adoption patterns are real and cited: subsurface-scattering skin, YAWAR blood particles, breathing-heart shader, KhipuKnot Reidemeister knot (R1/R2/R3 invariant) from the 46-leader 3D survey.
- Math substrate labeled in the scene caption: **749 declarations, 14 axioms, 163 sorries**, tag `lutar-v18.0.0`.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/szl-anatomy/commit/b38dc8a421a4dd922795f31bc6a106388ff597a3` (V1 `9914bce8`).
- 3D recipes: `450_3D_LEADERS_ADOPTION.md` (46 leaders, 12 paste-ready recipes).

### Stakes
Anatomy-3D is the Warhacker booth magnet: a defense audience that has seen a hundred slide decks has not seen a governance substrate rendered as a verifiable 3D body that turns red on a single byte of drift. It makes "formally verified anatomy" tangible in the ten seconds a judge walks past. For Series-A, it is the visual identity that lets a solo founder look like a team that ships — creative, but no bandaid.

### Invitation
**Open Anatomy-3D, click the heart, and read the 13-axis floor vector live; then corrupt one byte and watch the banner flip red.**

---

## 7. rosie-3d — 3D live-ecosystem brain-jack

### Origin
*Rosie-3D* is the living ecosystem view — every flagship Space as an organ in one breathing 3D mesh, wired by the spine and lit by live receipts. It extends Rosie's brain-jack from one decision to the whole body in motion. It is agentic ecology: the fabric shown not as a diagram but as a system that is alive and watchable.

### Mechanism
- Renders all 8 canonical Spaces (a11oy, amaru, sentra, killinchu, rosie, uds-demo, anatomy, README) as organs in a 3D-force-graph brain-jack mesh.
- Live wires animate PENDING→GREEN as receipts land; the **13-axis** gate is the choke point every wire passes through.
- The KANCHAY FBM halo, curve-particle flow, and InstancedMesh spine come from the cited 46-leader survey — real GPU techniques, not decoration for its own sake.
- The same disclosed math substrate underwrites the labels: **749 declarations, 14 unique axioms, 163 tracked sorries**, replay hash `bacf5443…631fc5`.
- Honest label: Wire D (W3C traceparent across the mesh) is **NOT YET IMPLEMENTED**; in-mesh wires are honest within a single Space, the cross-mesh propagation is disclosed as future work.

### Evidence
- Space: `huggingface.co/spaces/SZLHOLDINGS/rosie/commit/6da36b78e04ff8a90d990993c049973401a50c59` (3D ecosystem layer additive).
- 3D recipes: `450_3D_LEADERS_ADOPTION.md` recipes #3 (Bloom), #9 (3d-force-graph brain-jack), #12 (`<Detailed>` LOD).

### Stakes
Rosie-3D is the one-screen story for Warhacker and for Greene: the entire SZL fabric, every organ live, every wire receipted, in a single rotating view — the "single unified operational picture" defense buyers ask for, applied to the governance substrate itself. It compresses the whole pitch into a glance. For Series-A, it is the "this is a real system, not a deck" moment.

### Invitation
**Open Rosie-3D, rotate the ecosystem, and click any wire to read the receipt that turned it green.**

---

## 8. README — the SZLHOLDINGS org card

### Origin
The org card is the front door: *SZL Holdings builds a formally-verified 13-axis governance gate for agentic AI.* Hatun-Willay adds one honest paragraph of context beneath the locked banner, avatars, and emojis — never over them. It is the great telling in miniature: one paragraph that points at the whole proof.

### Mechanism
- States the wedge verbatim: a conjunctive (no-compensation) **13-axis** heart (`yuyay_v3`, replay hash `bacf5443…`) behind a 660-SLOC immune deadman (HUKLLA, 10 tripwires), a 20-line receipt ledger (YAWAR), and an 18-SLOC inline screen (SENTRA).
- Cites the math honestly: **749 declarations, 14 axioms, 163 tracked sorries**, now mirrored as four public HF Datasets; Λ uniqueness is an open **Conjecture**.
- Names the 8 canonical Spaces and the a11oy.code 7-tier router as the unifying fabric.
- Carries the honest migration label: the **9-axis** legacy still runs; the **13-axis** migration is the next ratchet.
- **Touches NOTHING locked:** banner, 5 painterly hero avatars, and animated emojis are untouched per HR-3 — the paragraph is inserted in the body only.

### Evidence
- Org card: `huggingface.co/spaces/SZLHOLDINGS/README/commit/cc8831acaa6d1c1f618f96b29e0104a7106d4f90`.
- Four math datasets: `lean-proofs-v1`, `canonical-formulas-v1`, `thesis-corpus-v18`, `doctrine-v10-v11`.

### Stakes
The org card is the first thing a Warhacker judge, an investor, or Andrew Greene sees when they open the HF org — it has ninety seconds to say "verifiable, honest, deployable" before they move on. One disciplined paragraph that points at four public datasets and eight live Spaces does more for Series-A credibility than any superlative. It is the proof that the telling and the math are the same thing.

### Invitation
**Open the SZLHOLDINGS org, read the one paragraph, then open any of the four datasets and verify a single number yourself.**

---

## 9. CHASKI — the reception organ (inbound relay / welcome + onboarding)

### Origin
*Chaski* is the Inca relay-runner who carried the quipu from post to post until the message arrived intact ([Wiktionary: chaski](https://en.wiktionary.org/wiki/chaski); [Wikipedia: Chasqui](https://en.wikipedia.org/wiki/Chasqui)). Here Chaski is the empire's front door — the organ that receives every newcomer and every inbound request, decays backpressure, and routes only what is routable. It is agentic hospitality: nothing enters the body except through a relay that can prove it had somewhere to go.

### Mechanism
- Chaski serves reception under `/api/{ns}/chaski/*` — `welcome`, `onboard/start`, `onboard/step`, `quickstart`, `heatmap` — with a 3D welcome scene that places the relay at the **eyes/face** of every cortex (anatomy-3d V3, ADDITIVE).
- Its PURIQ v13 sub-formula is **SF-13 Chaski-Puriq**: `Chaski(a) = exp(−κ·backpressure(a))·𝟙[routable(a)]`, a factor in `[0,1]` — reception can only gate harder, never inflate utility (INV-5; `PuriqLean.lean §9.1`, `chaski_factor_admissible`, `sorry`-tagged).
- Every reception action emits a **Khipu receipt** into the append-only SHA3-256 hash-chain (`get_dag("chaski")`, chain verified depth 7 in local tests); the DSSE signature is honestly a **PLACEHOLDER** until Sigstore is wired.
- It reuses the disclosed math substrate verbatim: **749 declarations, 14 unique axioms, 163 tracked sorries** — the 3 new v13 organ obligations live OUTSIDE that count (→ 166 only once instilled), exactly as the v12 invariants do.
- Honest label: the 13-axis `yuyay_v3` gate (replay hash `bacf54434f1a3bf2d758b27a62d5fd580ca4c8d3b180693573eeebcaea631fc5`) still governs what Chaski admits; Chaski is a gate IN FRONT of the heart, not a replacement for it.

### Evidence
- Module: `a11oy_organs/szl_chaski.py` registered via `register(app, ns="a11oy")` before the SPA catch-all; tab page `chaski.html`. All endpoints return 200 in `test_organs_results.json` (verdict GREEN, 0 failures).
- Anatomy: `huggingface.co/spaces/SZLHOLDINGS/szl-anatomy/commit/b38dc8a421a4dd922795f31bc6a106388ff597a3` (V1 `9914bce8`) — Chaski node added ADDITIVE in V3 patch `new_organs/anatomy_patch/`.
- Push target: `spaces/SZLHOLDINGS/a11oy` (`efb1f44d`) via **HfApi direct write** (NEVER GitHub Actions, per HR).

### Stakes
Chaski is the Warhacker first-impression organ: a defense reviewer who lands cold gets an onboarding relay that explains the fabric in seconds and proves backpressure is bounded before any compute runs — the difference between a demo that overwhelms and one that welcomes. It is the "package and deploy, not just prototype" courtesy Defense Unicorns expect at the door. For Series-A, a governed reception layer is the proof the system degrades gracefully under load instead of falling over.

### Invitation
**Open `/chaski`, start the onboarding relay, and watch the welcome Khipu receipt chain back to root — then push backpressure up and see the reception factor decay toward zero.**

---

## 10. WALLPA — the expression organ (governed voice / TTS narration)

### Origin
*Wallpa* is from Quechua *wallpay*, "to create / to fashion / to invent" ([Wiktionary: wallpay](https://en.wiktionary.org/wiki/wallpay)) — the organ that gives the empire a voice without letting that voice say more than the sanctioned act allows. It speaks the doctrine and the decisions aloud, in synthetic per-organ timbres, never a cloned human. It is agentic expression: the body can finally talk, but only within the envelope the gate already approved.

### Mechanism
- Wallpa serves voice under `/api/{ns}/wallpa/*` — `voices`, `speak` (POST), `speak/stream` (SSE), `narrate-doctrine` (POST, ~67.9 s) — placed at the **mouth/throat** of every cortex in anatomy-3d V3.
- **Open-source TTS only** (Coqui XTTS / OpenVoice / Piper / NVIDIA Riva free tier where applicable) with a deterministic synthetic-timbre WAV vocoder fallback that always returns real 16 kHz audio; **NO real-person voice cloning without consent** — the 6 voices (amaru-voice, yuyay-voice, killinchu-voice, hatun-willay, chaski-voice, wasi-rikuq-voice) are SYNTHETIC TIMBRES.
- Its PURIQ v13 sub-formula is **SF-14 Wallpa-Puriq**: `Wallpa(a) = 𝟙[render(a) ⊑ sanctioned(a)]·(1 − drift_out(a))`, a factor in `[0,1]` — expression can never exceed the sanctioned action (INV-6; `PuriqLean.lean §9.2`, `wallpa_no_inflation` + `wallpa_factor_admissible`, `sorry`-tagged).
- Every utterance emits a **Khipu receipt** (`get_dag("wallpa")`, chain verified depth 4 in local tests); same disclosed substrate: **749 declarations, 14 axioms, 163 sorries**, replay hash `bacf54434f1a3bf2d758b27a62d5fd580ca4c8d3b180693573eeebcaea631fc5`.
- Honest label: SLSA remains **L1** ("SLSA L3" is BANNED); the DSSE receipt signature is a **PLACEHOLDER**.

### Evidence
- Module: `a11oy_organs/szl_wallpa.py` registered via `register(app, ns="a11oy")`; tab page `wallpa.html`. `voices`/`speak`/`narrate-doctrine` all 200 in `test_organs_results.json` (narration_seconds 67.9).
- Push target: `spaces/SZLHOLDINGS/a11oy` (`efb1f44d`) via **HfApi direct write**.

### Stakes
Wallpa is the booth organ that makes a judge stop walking: the governance substrate narrates its own doctrine in a synthetic voice, and the subsumption factor proves it cannot say more than the gate approved — a tangible answer to "can your AI go off-script?" For Defense Unicorns and any authorization board, a voice that is provably bounded by the sanctioned action is the difference between a feature and a liability. For Series-A, open-source-only TTS with no unconsented cloning is the compliance posture that survives diligence.

### Invitation
**Open `/wallpa`, hit `narrate-doctrine`, and listen to the empire read its own LOCKED numbers — then raise output drift and watch the expression factor fall.**

---

## 11. WASI-RIKUQ — the house-watcher organ (advisory observability / chaos)

### Origin
*Wasi-Rikuq* is the house-watcher: *wasi* = "house" and *rikuq* = "the one who watches" (from *rikuy*, to see) ([Wiktionary: wasi](https://en.wiktionary.org/wiki/wasi); [Wiktionary: rikuy](https://en.wiktionary.org/wiki/rikuy)). It sits atop the head as a watchful eye over the whole empire — every flagship's health, every error budget, every incident — and it advises but never seizes the halt lever. It is agentic stewardship: the watcher that keeps the house, while HUKLLA alone keeps the right to stop it.

### Mechanism
- Wasi-Rikuq serves observability under `/api/{ns}/wasi-rikuq/*` — `dashboard`, `incidents`, `runbook`, `chaos` (POST), `health-of-the-empire` — polling each live flagship `/healthz` and rendering a 3D house-watcher atop every cortex.
- The `chaos` endpoint is **2-person Yuyay-gated**: a chaos experiment fires only when two distinct approvers clear the gate, a deliberate human-in-the-loop control on the one destructive action in the organ.
- Its PURIQ v13 sub-formula is **SF-15 Wasi-Rikuq-Puriq**: `Wasi(a) = (∏ₒ healthₒ(a))·𝟙[error-budget intact(a)]`, a factor in `[0,1]` — advisory only: it informs HUKLLA but never usurps it, so **HUKLLA stays the sole halt-authority** (INV-7; `PuriqLean.lean §9.3`, `wasi_does_not_usurp_halt` + `wasi_factor_admissible`, `sorry`-tagged).
- Every watch/incident/chaos action emits a **Khipu receipt** (`get_dag("wasi-rikuq")`, chain verified depth 7 in local tests); it consumes Wires D–H from the resilience layer for cross-organ health.
- Disclosed substrate verbatim: **749 declarations, 14 unique axioms, 163 tracked sorries**, tag `lutar-v18.0.0`; SLSA **L1**; Λ uniqueness remains a **Conjecture**.

### Evidence
- Module: `a11oy_organs/szl_wasi_rikuq.py` registered via `register(app, ns="a11oy")`; tab page `wasi-rikuq.html`. `dashboard`/`incidents`/`runbook`/`chaos`/`health-of-the-empire` all 200 in `test_organs_results.json`; local screenshot shows 5/5 up, health 1.000 GREEN, LOCKED 749/163.
- Resilience source: `resilience_observability/WIRES_D_TO_H_INTEGRATION.md` + `INCIDENT_RESPONSE_RUNBOOK.md`.
- Push target: `spaces/SZLHOLDINGS/a11oy` (`efb1f44d`) via **HfApi direct write**.

### Stakes
Wasi-Rikuq is the organ that answers the question every authorization board asks last and means most: "how do you know the system is healthy, and who is allowed to stop it?" A house-watcher that shows empire-wide health, gates chaos behind two humans, and explicitly defers the halt lever to HUKLLA is exactly the separation-of-duties defense reviewers and Andrew Greene's deployment path require. For Series-A, advisory observability with a hard 2-person gate on the destructive path is the operational maturity that turns a prototype into something a buyer can run in production.

### Invitation
**Open `/wasi-rikuq`, read the health-of-the-empire score, then try to fire a chaos experiment with one approver and watch the 2-person Yuyay gate refuse it.**

---

— Signed **Yachay** (CTO authority), PURIQ brain-trust, 2026-06-01. No mysticism. Math + story together. No bandaid.

---

### Addendum (2026-06-01, Yachay) — live instillation record

The three edge organs (cards 9–11) are now **LIVE on a11oy** and **rendered on anatomy-3d**,
not staged. Verified this session with the founder token (`betterwithage`, admin on SZLHOLDINGS):

- **CHASKI / WALLPA / WASI-RIKUQ endpoints** all return **200** on
  `https://szlholdings-a11oy.hf.space/api/a11oy/{chaski,wallpa,wasi-rikuq}/*`, each with a real
  Khipu receipt; WALLPA `/speak` returns real 16 kHz WAV; WASI-RIKUQ `/dashboard` polls all 5
  live flagship `/healthz` endpoints in real time; the 2-person Yuyay chaos gate enforces
  403 (1 approver) → 200 (2 distinct ≥0.90).
- **Tab pages** `/chaski`, `/wallpa`, `/wasi-rikuq` render (200).
- **anatomy-3d** (`SZLHOLDINGS/anatomy-3d`, the live anatomy Space — supersedes the earlier
  `szl-anatomy` reference) now contains the 3 organ nodes: CHASKI (cyan) at the eyes/face,
  WALLPA (purple) at the mouth/throat, WASI-RIKUQ (gold) atop the head, plus advisory wires
  I/J/K. ADDITIVE; LOCKED **749 / 14 / 163** preserved verbatim.
- Lean: `PuriqFormulaLean.lean §10 (namespace Puriq.EdgeOrgans)` adds `chaskiFactor`,
  `wallpaFactor`, `wasiFactor`, `puriqUtilityV13` and 5 `sorry`-tagged theorems
  (parse-verified clean, only the expected `sorry` warnings). The 3 v13 factor obligations
  live OUTSIDE the locked 163 (→ 166 once instilled).

— Signed **Yachay**. ADDITIVE only. No bandaid.
