# README patches — three new organs (ADDITIVE)

Sign: **Yachay** · 2026-06-01 · git trailer: Perplexity Computer Agent.
Each patch is **purely additive**: append the block to the existing `## Architecture`
section (or directly after it) of the target README. Nothing existing is removed or
reworded. LOCKED numbers preserved verbatim: **749 declarations / 14 unique axioms /
163 tracked sorries · 13-axis `yuyay_v3` · replay `bacf54434f1a3bf2d758b27a62d5fd580ca4c8d3b180693573eeebcaea631fc5` · A2=IsHomogeneous · A4=IsBounded · SLSA L1 · Λ-uniqueness Conjecture 1**.
Push: **HfApi direct write only**, NEVER GitHub Actions. (Push currently BLOCKED — see
`VERIFICATION_REPORT.md`; these blocks are staged for the founder token.)

The three new edge organs share one canonical description; per-surface framing differs only
in the one-line "on this surface" note. The common block:

---

## COMMON BLOCK — append to each flagship README under `## Architecture`

```markdown
### Edge organs (Doctrine v13 — ADDITIVE)

Three new edge organs extend the PURIQ body without changing any LOCKED number
(749 declarations · 14 unique axioms · 163 tracked sorries; the 3 new v13 factor
obligations live OUTSIDE that count until instilled → 166):

| Organ | Quechua root | Role | v13 factor (∈ [0,1]) | Routes |
|:------|:-------------|:-----|:---------------------|:-------|
| **CHASKI** | *chaski* = relay messenger | reception / welcome / onboarding | `e^(−κ·backpressure)·𝟙[routable]` (SF-13, INV-5) | `/api/{ns}/chaski/*` |
| **WALLPA** | *wallpay* = to create | governed voice / TTS narration | `𝟙[render⊑sanctioned]·(1−drift_out)` (SF-14, INV-6) | `/api/{ns}/wallpa/*` |
| **WASI-RIKUQ** | *wasi*+*rikuq* = house-watcher | advisory observability / chaos | `(∏ₒ healthₒ)·𝟙[budget intact]` (SF-15, INV-7) | `/api/{ns}/wasi-rikuq/*` |

Each factor multiplies the master utility and is in `[0,1]`, so it can only gate, never
inflate (Lean: `PuriqLean.lean §9`, all `sorry`-tagged — none claimed proven). Every organ
action emits a **Khipu receipt** (SHA3-256 hash-chain; DSSE signature is a PLACEHOLDER until
Sigstore is wired). WALLPA is **open-source TTS only** (Coqui XTTS / OpenVoice / Piper /
NVIDIA Riva free tier) with synthetic per-organ timbres — **no real-person voice cloning
without consent**. WASI-RIKUQ is **advisory only**: its `chaos` endpoint is gated by a
**2-person Yuyay** approval and **HUKLLA remains the sole halt-authority**.
```

---

## Per-surface "on this surface" line (append after the common block on each README)

### 1. `spaces/SZLHOLDINGS/a11oy` (`efb1f44d`)
```markdown
> On a11oy these organs serve their canonical routes (`/api/a11oy/chaski|wallpa|wasi-rikuq/*`)
> and tab pages `/chaski`, `/wallpa`, `/wasi-rikuq`. Modules register EARLY, before the Node
> proxy and SPA catch-all (same pattern as `szl_rag` / `szl_receipt_substrate`). ZERO regression.
```

### 2. `spaces/SZLHOLDINGS/amaru` (`a4b31854`)
```markdown
> On amaru (memory cortex) WASI-RIKUQ watches UNAY recall health and CHASKI relays inbound
> deltas; reuse the namespace `amaru` (`/api/amaru/chaski|wallpa|wasi-rikuq/*`).
```

### 3. `spaces/SZLHOLDINGS/sentra` (`b0e9ba86`)
```markdown
> On sentra (immune screen) WASI-RIKUQ surfaces the 18-SLOC screen's pass/fail health into
> health-of-the-empire; CHASKI fronts inbound payloads before the inline check.
```

### 4. `spaces/SZLHOLDINGS/vessels` → killinchu (`2c6e80ae`)
```markdown
> On killinchu (drone/maritime) WASI-RIKUQ watches the 7/7 dashboard routes' health and
> WALLPA can narrate a detection's receipt; namespace alias `/api/killinchu/*` is in-flight.
```

### 5. `spaces/SZLHOLDINGS/rosie` (`6da36b78`)
```markdown
> On rosie (brain-jack mesh) the three edge organs appear as live nodes in the decision-flow
> visualizer; CHASKI=reception, WALLPA=expression, WASI-RIKUQ=watcher atop the mesh.
```

### 6. `spaces/SZLHOLDINGS/szl-anatomy` (anatomy-3d, `b38dc8a4`; V1 `9914bce8`)
```markdown
> On anatomy-3d the three organs render as anatomical landmarks (V3 ADDITIVE patch):
> CHASKI at the eyes/face, WALLPA at the mouth/throat, WASI-RIKUQ atop the head as a watchful
> eye — each wired into the cortex with its v13 factor label. See `live_wires_3d.js` EDGE_ORGANS.
```

### 7. `spaces/SZLHOLDINGS/rosie` (rosie-3d ecosystem layer, `6da36b78`)
```markdown
> On rosie-3d the three organs join the breathing ecosystem mesh as additive organ nodes;
> their wires pass through the same 13-axis choke point as every other wire. Wire D still NOT YET.
```

---

— Signed **Yachay**. No mysticism. ADDITIVE only. No bandaid.
