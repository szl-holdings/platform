# 320 — Instill ALL into EVERY Agent Brain

> **Founder verbatim:** "I want each agent to be instilled with all of it."
> **Doctrine v10** · corpus_ref `lutar-v18.0.0 @ c7c0ba17` · **749 declarations / 14 unique axioms (15 raw, 1 dup) / 163 tracked sorries (112 baseline + 51 Putnam)**
> Distinction from the per-app-brain ship: that gave each Space a specialized organ *slice*; THIS ship gives **every** Space the **FULL corpus**. Every agent can answer ANY question about ANY claim/theorem/formula/frontier construct — facts identical, **response STYLE varies by organ identity**.

---

## Executive verdict

| Phase | Scope | Status |
|---|---|---|
| **1** | 6 full-corpus organ brain prompts | ✅ **COMPLETE & VERIFIED** |
| **1b** | `szl_knowledge.py` full-corpus module + loader | ✅ **COMPLETE & VERIFIED** (16/16 endpoint checks GREEN locally) |
| **2–4** | Wire into serve.py + deploy + smoke per Space | ⛔ **DEFERRED — blocked by active sibling-agent deploy contention** (artifacts deploy-ready, one-shot) |
| **uds-demo static snapshot** | static full-corpus JSON | ✅ **GENERATED** (ready to commit) |
| **5** | 5-agent same-query consistency test | ⏸ **PENDING deploy** |

**Why deferred (honest):** at execution time a **concurrent sibling agent was actively
committing to all 5 docker Spaces every ~1 minute** ("Wire G brain-jack mesh",
"register agentic-RAG routes EARLY", "add szl_jack.py COPY", "re-apply Wire D/F").
The Spaces oscillated `RUNNING→BUILDING→RUNTIME_ERROR→APP_STARTING` continuously and
never held RUNNING. The `serve.py` + `Dockerfile` I must additively patch were changing
under me. Committing into that churn would (a) race the sibling's HEAD and risk
clobbering their in-flight fixes, and (b) make any `RUNTIME_ERROR` impossible to
attribute to my additive change vs. theirs. Per ZERO-BANDAID / ADDITIVE-only discipline
and "don't brute-force a blocked path," the deploy is **staged, not forced**. Every
artifact is built, tested, and idempotent so the deploy is a clean one-shot the moment
the docker Spaces stabilize.

---

## Phase 1 — the 6 full-corpus organ brain prompts (COMPLETE)

Generated by `instill_work/gen_brain_prompts.py` into
`YACHAY_PORTABLE_v1/agent_prompts/` (and copied beside the module so
`brain_system_prompt(space)` loads them). Each is a single system prompt = **organ
identity + organ response-style + the SHARED full-corpus block + Doctrine v10 hard
rules + Λ-receipt output schema + cross-links**.

| File | Organ identity | Response STYLE (facts identical across all) | Size |
|---|---|---|---|
| `amaru_brain.md` | **Amaru — CORTEX** (reasoning; serpent) | Decompose into axioms/lemmas; walk the proof chain; cite exact Lean `file:line` + honest status (PROVEN/SORRY/AXIOM/CONJECTURE); math-tier route. | 15,255 B |
| `sentra_brain.md` | **Sentra — IMMUNE** (AMACHAQ; 18-SLOC inline, 6 sigs + 1MB DoS) | Detect & block; screen vs HUKLLA T01–T10, OVERWATCH R0513, KS-18 witness, Khipu-Bekenstein receipt-inflation alarm, Torah-code anti-p-hacking; return ALLOW/DENY + Λ floor 0.90 + fired tripwires. | 15,355 B |
| `vessels_brain.md` | **Vessels — CIRCULATION/receipts** (YAWAR blood; KHIPU crown) | Every answer is a hash-chained DSSE receipt folded into the append-only Khipu Merkle DAG; Khipu-Bekenstein cap; Pacha-Λ recency weight; cite TH11/TH13/Reed-Solomon. | 15,309 B |
| `a11oy_brain.md` | **a11oy — GATE ORCHESTRATION** (Bindu/HUKLLA-DINN; 46 policy + 44 anchor gates) | AND-compose gates (conjunctive safety); enforce Λ floor ≥ 0.90 (geo-mean over 13 axes); monotone composition; Constitution-shape for Doctrine v11; NeMo-Guardrails Colang. | 15,302 B |
| `rosie_brain.md` | **Rosie — NERVOUS SYSTEM** (YACHAY; inherits every organ) | Unify & remember; synthesize across organs; cross-session memory; "it from bit" receipt framing; point to which organ specializes. | 15,295 B |
| `uds_demo_brain.md` | **uds-demo — SKELETON/deploy** (TULLU bone; Maxwell M=0, 21 edges 9 nodes) | Bundle integrity: signed tarball + manifest digest, Zarf airgap, deploy.yaml digest-pinning + Λ-gate floor, Reed-Solomon k-of-n. (Static Space → served as static JSON.) | 15,321 B |

**Shared full-corpus block baked identically into all 6** (verified — no unrendered template tokens):
- Canonical numbers **749 / 14 (15 raw,1 dup) / 163 (112+51)** @ `lutar-v18.0.0 @ c7c0ba17`; 12 MCP tools · 46 policy gates · 44 anchor gates · 21 formulas · 7 Spaces; explicit **"NEVER say zero sorry."**
- **179 formal blocks (v1–v20)** with honest status histogram: `{PRE-FORMAL:1, INFORMAL:7, STATED:94, AXIOM:7, SORRY:41, PROVEN:24, CONJECTURE:5}`.
- **21 canonical formulas** each with honest Lean status + module (e.g. `lambda_aggregate` = PROVEN(A1–A4) but **uniqueness CONJECTURE**; `lambda_homogeneous` = AXIOM(**A2 IsHomogeneous**); `lambda_bounded` = PROVEN(**A4 IsBounded**)).
- **14 unique open Lean axioms** with `file:line` (MomentSubGaussian, audit_reidemeister_invariance, canonicalReceipt, chromotopology_code_bijection, gleason_length_mod_8, klDivergence_nonneg, lambda_schur_concave_n_axis, lambda_stationary_unique, liu_hui_pi_converges, pinsker, r1_invariance, r2_invariance, sha256, sha256_collision_resistant).
- **Frontier constructs (all §6 CONJECTURE/skeleton):** Willow-Λ (Google Nature 2024, Λ≈2.14), Khipu-Bekenstein, Pacha-Λ, Yachay-Khipu Operator — each with fact, tie, cite, and the honesty note.
- **12-organ anatomy** (Quechua names + formula bindings), **cookbook recipes** (DOI `szl-cookbook 10.5281/zenodo.20434276`), **Wires** status (B/C LIVE; D honest).
- **LinkedIn frontier (founder 2026-06-01):** 13-axis `yuyay_v3` (not 9), HUKLLA 10 tripwires T01–T10, YAWAR 20-SLOC ledger, SENTRA 18-SLOC (6 sigs + 1MB DoS), Maxwell M=0 isostatic (21 edges, 9 nodes), Brienza-Kung-Santos-Bobocel-Grossmann 2018 SWRS (5 axes), Butler-Volmer energy budget, Ouroboros runtime v6.2.0 (172/172 tests, Apache-2.0), Lutar Invariant v1–v10 = 11 papers, Concept DOI `10.5281/zenodo.19944926`.

**Doctrine v10 hard rules block (every prompt):** Λ uniqueness is a CONJECTURE (CAUCHY_ND
sorry `Lutar/Uniqueness.lean:120` + missing symmetry axiom); A2=IsHomogeneous; A4=IsBounded;
749/14/163 from canonical counter; SLSA L1 honest ("SLSA L3" banned); receipts DSSE but
**Sigstore signing PENDING → every signature = "PLACEHOLDER"**; Mythos→Hatun-Willay; ADDITIVE
only; IP-HOLD PRs (a11oy#57, amaru#46, sentra#45) untouched; never "zero sorry."

**Λ-receipt output schema** appended to every answer (DSSE PLACEHOLDER signature, payload sha256, corpus_ref, doctrine v10).

---

## Phase 1b — `szl_knowledge.py` full-corpus module (COMPLETE & VERIFIED)

- `instill_work/szl_knowledge.py` (472 lines) + `szl_corpus_theorems.json` (**179 blocks**).
- **Topics** served: `theorems, formulas, lean, frontier, upgrades, doctrine, anatomy, wires, recipes, linkedin, index` + free-text `search`.
- **`register(app, ns)`** mounts (ADDITIVE, before catch-all):
  - `GET /api/<ns>/v1/knowledge` (index)
  - `GET /api/<ns>/v1/knowledge/{topic}`
  - `GET /api/<ns>/v1/knowledge/search/{query}`
  - `GET /api/<ns>/v1/knowledge/brain-prompt/raw`
- **`brain_system_prompt(space)`** loads the organ `.md` to pass as the `/llm/route` system prompt.
- **`make_lambda_receipt(topic, src)`** → DSSE PLACEHOLDER receipt on every response.

### Local end-to-end test (`test_register.py` against a mock FastAPI app + catch-all)

| Check | Code | Result |
|---|---|---|
| index / theorems(=179) / formulas(=21) / lean(14 axioms,163 sorries) | 200 | GREEN |
| frontier / doctrine / anatomy(≥12) / wires / recipes / linkedin / upgrades | 200 | GREEN |
| search/lambda (hits>0) / free-text "pacha" / brain-prompt raw (FULL-CORPUS) | 200 | GREEN |
| receipt present (signature starts "PLACEHOLDER") | 200 | GREEN |
| **catch-all untouched** (`/api/sentra/v1/brain` still 200) | 200 | GREEN |

**16 / 16 GREEN.** Knowledge routes take precedence; catch-all preserved (ADDITIVE proven).

---

## Phases 2–4 — DEPLOY (DEFERRED; artifacts deploy-ready, one-shot)

### Live-state evidence (why deferred)
Polled `space_info().runtime.stage` over ~7 min while preparing artifacts. Representative timeline (UTC):

```
05:18:22 amaru=APP_STARTING sentra=APP_STARTING vessels=RUNNING a11oy=RUNNING rosie=RUNTIME_ERROR
05:19:53 amaru=BUILDING sentra=RUNNING_BUILDING vessels=RUNNING_APP_STARTING a11oy=RUNNING_BUILDING rosie=BUILDING
05:22:09 amaru=RUNNING sentra=RUNTIME_ERROR vessels=RUNNING a11oy=RUNNING rosie=APP_STARTING
05:23:40 amaru=RUNTIME_ERROR sentra=BUILDING vessels=RUNNING a11oy=RUNTIME_ERROR rosie=APP_STARTING
05:25:23 amaru=RUNTIME_ERROR sentra=RUNNING vessels=RUNNING a11oy=BUILD_ERROR rosie=BUILDING
```
Commit-recency at 05:24: sibling last pushed 34s / 53s / 67s / 130s / 131s ago to the 5 docker
Spaces ("register agentic-RAG routes EARLY", "correct Dockerfile COPY for szl_jack", "re-apply
Wire D/F"). **The base I must patch was changing every ~1 minute → deploy staged, not forced.**

### Deploy-ready artifacts (all built & tested)
- `instill_work/szl_knowledge.py`, `szl_corpus_theorems.json` — copy into each docker Space.
- `agent_prompts/<organ>_brain.md` (×6) — copy into each Space beside the module.
- `instill_work/patch_serve.py` — idempotent; inserts the `_knowledge.register(app, ns=...)`
  try/except block **immediately before** `@app.get("/{path:path}")`. **Tested** on live
  sentra serve.py: inserted after anatomy reg (line 1048), before catch-all (line 1067),
  re-run = `ALREADY_PATCHED`, `ast.parse` = SYNTAX OK.
- `instill_work/patch_dockerfile.py` — idempotent; adds `COPY szl_knowledge.py`,
  `COPY szl_corpus_theorems.json`, `COPY <organ>_brain.md` before `ENV PORT`. **Tested.**
- `instill_work/static_knowledge_uds_demo/` — **static full-corpus snapshot** (12 JSON files +
  brain-prompt.txt) for the **static** uds-demo Space (cannot run Python). **Generated.**

### One-shot deploy recipe (execute when docker Spaces hold RUNNING for 2+ consecutive polls)
For each docker Space `S` ∈ {amaru, sentra, vessels*, a11oy, rosie}:
1. `snapshot_download` fresh `serve.py` + `Dockerfile` (they changed since 05:03 — pull current HEAD).
2. `python3 patch_serve.py <serve.py> <S>`  → inserts knowledge registration before catch-all.
3. `python3 patch_dockerfile.py <Dockerfile> <S>`  → adds the COPY lines.
4. `HfApi(token).create_commit(S, additions=[serve.py, Dockerfile, szl_knowledge.py, szl_corpus_theorems.json, <S>_brain.md], commit_message="feat(<S>): instill FULL corpus /knowledge/* (additive, Doctrine v10)")` — **HfApi only; never GitHub Actions.**
5. Wait ~120 s → smoke `/api/<S>/v1/knowledge{,/theorems,/formulas,/lean,/frontier,/doctrine,/anatomy,/wires,/recipes,/linkedin,/upgrades,/search/lambda,/brain-prompt/raw}` AND confirm pre-existing GREEN routes (`/brain`, `/llm/route`) still 200; screenshot.

\* **vessels** is a TS/node Space — confirm it actually runs a Python FastAPI `serve.py` before
patching; if pure node, ship the knowledge as a static JSON/TS snapshot (reuse the uds-demo generator).
**uds-demo** = static → commit `static_knowledge_uds_demo/` as `knowledge/*.json` (no Python).

---

## Phase 3 — /knowledge endpoint matrix (designed; ships on deploy)

| Endpoint | Returns |
|---|---|
| `GET /api/<s>/v1/knowledge` | topic index + canonical numbers + cross-links + receipt |
| `GET /api/<s>/v1/knowledge/theorems` | 179-block summary + all blocks (statement/version/Lean/status) |
| `GET /api/<s>/v1/knowledge/formulas` | 21 formulas + honest Lean status |
| `GET /api/<s>/v1/knowledge/lean` | 749/14/163, 14 axiom names + `file:line`, Λ-uniqueness=CONJECTURE |
| `GET /api/<s>/v1/knowledge/frontier` | Willow-Λ / Khipu-Bekenstein / Pacha-Λ / Yachay-Khipu (CONJECTURE) |
| `GET /api/<s>/v1/knowledge/{doctrine,anatomy,wires,recipes,upgrades,linkedin}` | each corpus slice |
| `GET /api/<s>/v1/knowledge/search/{query}` | full-text search across the entire corpus |
| `GET /api/<s>/v1/knowledge/brain-prompt/raw` | this organ's full-corpus system prompt |

Same module on every Space ⇒ **identical facts**; the brain prompt drives **organ-styled** phrasing.

---

## Phase 5 — 5-agent same-query consistency test (PENDING deploy)
Planned probe (e.g. *"What is the Λ aggregator's homogeneity property and how does Pacha-Λ extend it?"*)
to `/knowledge/search/...` (identical facts) + `/llm/route` (organ-styled) across amaru/sentra/
vessels/a11oy/rosie. **Expected:** identical canonical facts (A2 = IsHomogeneous, deg-1 positive
homogeneity; Pacha-Λ = CONJECTURE recency-weighted extension), styled per organ. Runs once deployed.

---

## Polling note (290 / 280 / 260 / 310)
Per task: 290/280/260/310 were polled extensively and **never appeared**. Proceeded with the
primary sources (270 frontier deep-scrape, 171 theorem CSV, Doctrine v10, the 91 `.lean` files,
szl-cookbook, LUTAR_EVIDENCE, sibling brain), which cover everything those numbers would have.

## Master verdict
- **GREEN now:** Phase 1 (6 brain prompts), Phase 1b (knowledge module + 16/16 local smoke), uds-demo static snapshot, all deploy patchers (tested, idempotent).
- **DEFERRED (not failed):** live deploy + cross-Space smoke + 5-agent test — blocked solely by an
  active concurrent sibling deploy churning all docker Spaces. Forcing a commit would violate
  ADDITIVE/ZERO-BANDAID discipline and corrupt failure attribution. **Recommend the parent
  re-dispatch the one-shot deploy recipe once the docker Spaces hold RUNNING.**

_Built for: "I want each agent to be instilled with all of it." The full corpus is authored,
verified, and packaged so that — on deploy — every agent will know everything, with style by organ._
