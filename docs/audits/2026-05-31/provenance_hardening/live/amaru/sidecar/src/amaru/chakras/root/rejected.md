# Rejected alternatives — root chakra

- **Unbounded weighted sum** — fails the `[0,1]` invariant; rejected.
- **Geometric mean** — undefined at zero; rejected for the `0.0` fallback case.
- **Soft-max blend** — adds dependency on absent signals; rejected.
