# LinkedIn — 2026-05-27

**Author:** Stephen Paul Lutar · SZL Holdings
**Audience:** founders, defense/dual-use operators, UDS / Defense Unicorns community

---

Quiet two weeks at SZL Holdings. Receipts, not slides.

**Vessels** now streams a real dark-vessel signal mesh off live AIS feeds (Digitraffic + BarentsWatch), not a synthetic stand-in. Provider provenance is surfaced in the UI, AIS-gap Λ-floors get hash-chained voyage receipts, and the endpoint shape is locked with a snapshot test so we can't silently regress on Defense-Unicorns pull partners.

**A11oy** wired the perception-loop and sequence-pipeline primitives into the reviewer flow and reliquary. Raw frame bytes never enter the envelope — that invariant is now enforced by a serialization test, not a code-review note. Reviewer presence and peak detection are *mixed* into the AMI signal (max for N/D, multiplicative damper for G), never overwritten — same discipline as the rest of the doctrine.

**Warhacker** got two upgrades on the same day: every demo run is now replayable from its trace ID, and the bundle×detector matrix has its own regression tests so a Lane 1 receipt can't lie about which signed bundle it came from. The hashes and signer DIDs you see in the UI are the real ones from the release artifacts.

**Sparse-attention synthesis** shipped as the fourth research absorption (MiniMax M1/M2, DeepSeek NSA, Moonshot MoBA, FlashAttention, FLA). The non-obvious lesson from MiniMax M2 reverting to full attention: hybrid-sparse wins benchmarks but loses multi-hop reasoning at scale, so the SZL absorption is gated by a contradiction probe with fail-up-to-full escalation. Twelve new `sparse.*.v1` receipt classes.

**Operations.** All eight artifact workflows green. Zero high/critical security alerts open. Doctrine v6 scanner sweep clean. Risk-formula drift clean. PR queue cleared.

**UDS posture.** Existing per-product bundles (a11oy `uds-v0.1.1`, sentra `uds-v0.2.0`, amaru / rosie / vessels `uds-v0.1.0`) are current — the kernels they ship didn't change. The new perception/bio and sparse-attention work lives in shared SZL packages today; rolling them into a `uds-v0.2` bundle expansion is a planned next cut, not a hot-fix.

The pattern under all of this is the same: every claim has a receipt, every receipt has a signer, every signer survives a contradiction probe. That's the only way I know how to build for operators who can't afford to be lied to.

— Stephen
