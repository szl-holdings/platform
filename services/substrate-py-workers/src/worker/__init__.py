"""
substrate-py-workers — SZL Holdings Python Worker Fleet

Executes stages tagged runtime="python" from the Substrate execution engine.
Speaks the typed wire protocol (protocol version 1.0) over HTTP.
"""

__version__ = "1.0.0"
PROTOCOL_VERSION = "1.0"
