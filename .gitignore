# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

# Replit internal
.cache/
.local/
.canvas/
.cursor/
.github/instructions/

# GitHub Actions workflow files — excluded because the GitHub OAuth token does not
# have "workflow" scope; workflows are managed directly on GitHub.
.github/workflows/

# Attached assets (user-pasted payload dumps)
attached_assets/

# Environment files
.env
.env.local
.env.*.local
*.env

# Noisy directories — quarantine from public mirror
.archive/
.mirror-staging-test/
.git-rewrite/
backups/
exports/
scratch/
temp/
*.bak
*.backup
*.sql.gz

# Turbo cache (generated build artifacts)
.turbo/

# Transient runtime output
nohup.out
*.out
*.log

# Root-level zip archives (all moved to archive/ — add new zips there, never at root)
/*.zip

# Root-level build scripts (relocated to archive/scripts/)
/build_carousel.py
/build_video.sh

# Social content and launch shots (relocated to archive/ by Zero-Gap Track 1)
/LINKEDIN-LAUNCH/
/launch-shots/

# Internal / non-public content
social-content/
spfx-webparts/
test-results/

# Large non-code assets — removed from tracking (task #2687); do not re-add
/deliverables/
/output/
/screenshots/
archive/phase-a/deliverables/
archive/phase-a/output/
archive/phase-a/screenshots/

# Mirror validation reports (generated)
scripts/public-mirror/mirror-report.md
docs/audit/public-mirror-report.md

# Playwright generated output
playwright-report/

# Database dumps
*.dump
*.pgdump

# ── Credential & secrets hardening ────────────────────────────────────────────
# Firebase / Google mobile credentials — never commit real files
google-services.json
GoogleService-Info.plist
# Google service-account keys
service-account*.json
google-play-service-account.json
*-service-account.json
*_service_account*.json
# Android signing keystores
*.keystore
*.jks
# iOS / macOS signing
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile
# EAS / Expo secrets
eas-cli-local-build-plugin-*
.eas/
# Generic secret / credential patterns
*secret*.json
*credentials*.json
*private_key*.json
# Additional .env variants not already listed
.env.*
!.env.example
!.env.*.example

# Auto-generated solution briefs (regenerated by scripts/post-merge.sh)
artifacts/szl-holdings/public/briefs/*.pdf
artifacts/szl-holdings/public/briefs/index.json

# Investor-facing PDFs (tracked in repo for version history)
# demo-assets/szl-holdings-investor-carousel.pdf

# Local gitleaks binary (installed on-demand for scanning; not committed to source)
gitleaks

# ── A11oy Doctrine additions (Task #3481) ─────────────────────────────────────
# Build output directory
build
# Next.js build output
.next
# Raw screenshot captures (processed copies go in docs/assets/screenshots/current/)
screenshots/raw

# ── Rehaul 2/9 — Repo hygiene additions (Task #2937) ──────────────────────────
# Launch-series unpacked dirs (removed; canonical: audit/launch/)
/X-LAUNCH-SERIES/
/a11oy-launch-content/
# Demo asset exports (removed; carousel/screenshots not committed)
/demo-assets/
# Root launch/ directory (content migrated to audit/launch/)
/launch/
# media/ root (screenshots migrated to docs/media/screenshots/)
/media/screenshots/
/media/brand-kit/
# Root-level screenshot and capture dirs (stray captures; use docs/media/screenshots/)
/launch-shots/
/screenshots/
# Generated output and backup dirs
/output/
/backups/
/deliverables/
# Social and SharePoint content dirs (not source code)
/social-content/
/spfx-webparts/
# Root-level build/generation scripts (noise at repo root — see archive/scripts/)
/build_carousel.py
/build_video.sh


# SZL Holdings public-attestation runtime data (Ed25519 private key, runs ledger)
.szl-public-runs/

# Substrate fleet secrets (never commit)
.env.substrate

# FORGE signing keys — private key material; never commit
.forge-skill-keys/
**/forge-keys/ed25519-private.pem
artifacts/api-server/.data/
artifacts/api-server/.data/agi-forecast-snapshot.json
