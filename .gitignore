# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

# Replit internal
.cache/
.local/
.canvas/
.cursor/
.github/instructions/

# Attached assets (user-pasted payload dumps)
attached_assets/

# Environment files
.env
.env.local
.env.*.local
*.env

# Noisy directories — quarantine from public mirror
.archive/
.mirror-staging-test/
.git-rewrite/
backups/
exports/
scratch/
temp/
*.bak
*.backup
*.sql.gz

# Turbo cache (generated build artifacts)
.turbo/

# Transient runtime output
nohup.out
*.out
*.log

# Large zip archives at repo root (quarantined to archive/ — do not re-add to root)
/01-thursday-intro.zip
/02-sunday-deep-dive.zip
/03-monday-operator-lens.zip
/LINKEDIN-LAUNCH.zip
/X-LAUNCH-SERIES.zip

# Root-level build scripts (relocated to archive/scripts/ by Zero-Gap Track 1)
/build_carousel.py
/build_video.sh

# Social content and launch shots (relocated to archive/ by Zero-Gap Track 1)
/LINKEDIN-LAUNCH/
/launch-shots/

# Internal / non-public content
social-content/
spfx-webparts/
test-results/

# Large non-code assets — removed from tracking (task #2687); do not re-add
/deliverables/
/output/
/screenshots/
archive/phase-a/deliverables/
archive/phase-a/output/
archive/phase-a/screenshots/

# Mirror validation reports (generated)
scripts/public-mirror/mirror-report.md
docs/audit/public-mirror-report.md

# Playwright generated output
playwright-report/

# Database dumps
*.dump
*.pgdump

# ── Credential & secrets hardening ────────────────────────────────────────────
# Firebase / Google mobile credentials — never commit real files
google-services.json
GoogleService-Info.plist
# Google service-account keys
service-account*.json
google-play-service-account.json
*-service-account.json
*_service_account*.json
# Android signing keystores
*.keystore
*.jks
# iOS / macOS signing
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile
# EAS / Expo secrets
eas-cli-local-build-plugin-*
.eas/
# Generic secret / credential patterns
*secret*.json
*credentials*.json
*private_key*.json
# Additional .env variants not already listed
.env.*
!.env.example
!.env.*.example

# Auto-generated solution briefs (regenerated by scripts/post-merge.sh)
artifacts/szl-holdings/public/briefs/*.pdf
artifacts/szl-holdings/public/briefs/index.json

# Investor-facing PDFs (tracked in repo for version history)
# demo-assets/szl-holdings-investor-carousel.pdf

# Local gitleaks binary (installed on-demand for scanning; not committed to source)
gitleaks
