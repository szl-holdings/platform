auto-install-peers=false
strict-peer-dependencies=false

# GitHub Packages — @szl-holdings scope (only needed for publishing; all packages are workspace-local in dev)
# NODE_AUTH_TOKEN is only required when publishing packages to GitHub Packages Registry.
# In development, this registry is used for resolution only and auth is not required.
@szl-holdings:registry=https://npm.pkg.github.com
