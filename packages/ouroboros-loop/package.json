{
  "name": "@workspace/ouroboros-loop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "Ouroboros closure operator O: X → X with Λ-gate, Newton fluxions-receipt, Gauss least-squares forecast, Bekenstein bound, dual-witness, and receipt chain composed into a single reusable runtime.",
  "scripts": {
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@workspace/ouroboros-gauss": "workspace:*",
    "@workspace/ouroboros-invariant": "workspace:*",
    "@workspace/ouroboros-newton": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  }
}
