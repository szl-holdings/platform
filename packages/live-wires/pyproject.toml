[project]
name = "szl-live-wires"
version = "0.1.0"
description = "Live Wires — 3D wire viewer + fanout hub"
license = { text = "Apache-2.0" }
authors = [{ name = "Yachay", email = "yachay@szlholdings.dev" }]
requires-python = ">=3.10"
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
