# @workspace/nexus-mcp

> ⚠️ **Canonical MCP server: [`szl-holdings/hatun-mcp`](https://github.com/szl-holdings/hatun-mcp).**
> See [`CANONICAL.md`](./CANONICAL.md) for the relationship and fold-in plan. This
> notice is additive; no code is changed and nothing is deleted.

`nexus-mcp` is a TypeScript **MCP SDK foundation** — the official
`@modelcontextprotocol/sdk` wrapped with SZL governance (Guardian policy evaluation,
proof-chain audit, tenant isolation, approval gates, and the 2025 spec innovation
layer: Sampling, Elicitation, Tasks, Apps, Instructions, Discovery, Roots).

It is a **library, not the shipped MCP server.** The fleet's live, canonical MCP
**server** is the standalone [`szl-holdings/hatun-mcp`](https://github.com/szl-holdings/hatun-mcp)
(spec-compliant Streamable HTTP transport, governed tool catalog).

---

Doctrine v11 LOCKED · 749/14/163 · kernel c7c0ba17 · Λ = Conjecture 1 (never a theorem)

Signed-off-by: Stephen P. Lutar Jr. <stephenlutar2@gmail.com>
