{
  "name": "@szl-holdings/sentra-detector-sdk",
  "version": "0.1.0",
  "description": "Sentra Detector SDK — canonical Detector / Finding / DetectorRun contracts and sidecar wire protocol shared by the api-server, TS detectors, and the Python sidecar.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
