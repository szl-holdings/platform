{
  "name": "@szl-holdings/domain-claims",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claim resolver utilities for SZL Holdings surfaces. Wraps public-claims registry values with truth-value labels so every surface displays consistent, audit-traceable metrics.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome lint ./src",
    "lint:ci": "biome lint ./src",
    "format": "biome format --write ./src"
  },
  "dependencies": {
    "@szl-holdings/platform-registry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
