{
  "name": "@szl-holdings/model-passport",
  "version": "0.0.0",
  "private": false,
  "description": "Model Passport spec — signed, portable per-model-variant identity and governance artifact (types, JSON schema, Ed25519 sign/verify, parsePassport)",
  "repository": {
    "type": "git",
    "url": "https://github.com/szl-holdings/szl-holdings-platform.git",
    "directory": "packages/model-passport"
  },
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./crypto": "./src/crypto.ts",
    "./resolver": "./src/resolver.ts",
    "./seed": "./src/seed.ts"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  },
  "scripts": {
    "lint": "biome lint ./src",
    "lint:ci": "biome lint ./src",
    "format": "biome format --write ./src"
  }
}
