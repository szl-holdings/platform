{
  "name": "@szl-holdings/unified-kernel",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "description": "Unified kernel — the 19 SZL theses, every formula, every Lean theorem-name, and every governance primitive wired as code modules through one signed bootable kernel. The theses ARE the software.",
  "author": "Stephen P. Lutar Jr. <stephenlutar2@gmail.com> (ORCID 0009-0001-0110-4173)",
  "license": "BSL-1.1",
  "exports": {
    ".": "./src/index.ts",
    "./kernel": "./src/kernel.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "boot": "tsx src/boot-demo.ts"
  },
  "dependencies": {
    "@szl-holdings/ouroboros": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:",
    "typescript": "^6.0.3",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20"
  }
}
