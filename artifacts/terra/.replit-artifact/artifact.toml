kind = "web"
  previewPath = "/terra/"
  title = "Terra — Real Estate Intelligence"
  version = "1.0.0"
  id = "artifacts/terra"
  router = "path"

  [[integratedSkills]]
  name = "react-vite"
  version = "1.0.0"

  [[services]]
  name = "web"
  paths = [ "/terra/" ]
  localPort = 6099

  [services.development]
  run = "bash -c 'cd /home/runner/workspace/artifacts/terra && exec node_modules/.bin/vite --config vite.config.ts --host 0.0.0.0'"

  [services.production]
  build = [ "pnpm", "--filter", "@workspace/terra", "run", "build" ]
  publicDir = "artifacts/terra/dist/public"
  serve = "static"

  [[services.production.rewrites]]
  from = "/*"
  to = "/index.html"

  [services.env]
  # Shared proxy port (9090) is hardcoded in vite.config.ts; PROXY_PORT env var is not used.
  VITE_PORT = "6099"
  BASE_PATH = "/terra/"
