# DEPRECATED

This app directory is a duplicate of `artifacts/firestorm` and has been deprecated.

The canonical Aegis — Unified Defense & Intelligence Command app is at `artifacts/firestorm` (preview path: `/firestorm/`).

**Do not add new features to this directory.** It is scheduled for removal from the artifact registry.

See `ops/cleanup/archive-and-deprecate.md` for the full deprecation register.
