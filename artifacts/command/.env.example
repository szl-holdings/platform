# Unified Command — Environment Variables
# Copy to .env.local and fill in values for your environment.
# NEVER commit .env.local to version control.

# ── API / Backend ──────────────────────────────────────────────────
# Base URL of the API server (used for all backend requests).
# In development this is typically the Replit dev domain + /api.
VITE_API_BASE_URL=https://szlholdings.com/api

# ── Deployment Environment ─────────────────────────────────────────
# Identifies the deployment environment: development | staging | production
# Used to conditionally show debug panels or banner labels.
VITE_DEPLOY_ENV=development

# ── Web Analytics ──────────────────────────────────────────────────
# Domain registered in Plausible dashboard for page-view analytics.
# Leave blank to disable Plausible.
VITE_PLAUSIBLE_DOMAIN=szlholdings.com

# ── Demo / Sandbox Mode ────────────────────────────────────────────
# Set to "true" to enable unauthenticated demo access (dev/staging only).
VITE_IS_DEMO=false

# ── Substrate Gateway ──────────────────────────────────────────────
# Set to "true" to enable Substrate demo/mock mode (no live gateway required).
VITE_SUBSTRATE_DEMO_MODE=false

# WebSocket URL for the Substrate agentic gateway.
# In development this is typically the Replit dev domain + /substrate.
VITE_SUBSTRATE_GATEWAY_URL=wss://szlholdings.com/substrate

# === Auto-added by CI gap-closure (PR #75) ===
# Document or unset each below; values are placeholders.
# Used in: artifacts/api-server/src/lib/pulse-org-channel-adapters.ts ✅ carlota-jo example : artifacts/carlota-jo/.env.example sour
VONAGE_API_SECRET=
# Used in: artifacts/command/vite.config.ts
PORT=
# Used in: artifacts/command/src/infrastructure/pages/substrate-inference.tsx
VITE_SUBSTRATE_API_KEY=
# === Auto-added by CI gap-closure ===
# artifacts/command/src/infrastructure/pages/substrate-inference.tsx
VITE_SUBSTRATE_INFERENCE_URL=
