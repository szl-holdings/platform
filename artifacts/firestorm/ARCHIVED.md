# Archived — Firestorm

This artifact has been archived and is no longer active.
All source files, build outputs, and config have been removed.
The domain logic lives in the API server (`artifacts/api-server/src/routes/firestorm*`).
