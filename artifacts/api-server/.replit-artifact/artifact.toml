kind = "web"
previewPath = "/api/"
title = "API Server"
version = "1.0.0"
id = "artifacts/api-server"
router = "path"

[[services]]
name = "api"
paths = [ "/api/", "/nexus/" ]
localPort = 8080

[services.development]
run = "pnpm --filter @workspace/api-server run dev:fast"

[services.env]
PORT = "8080"
