/** Stub for continuum-runtime group — placeholder until full implementation lands. */
import type { IRouter } from 'express';

export function register(_router: IRouter): void {
  // No routes registered yet.
}
