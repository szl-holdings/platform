kind = "web"
previewPath = "/inca/"
title = "INCA AI Research Command Center"
version = "1.0.0"
id = "artifacts/inca"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/inca/" ]
localPort = 24500

[services.development]
run = "pnpm --filter @workspace/inca run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/inca", "run", "build" ]
publicDir = "artifacts/inca/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24500"
BASE_PATH = "/inca/"
