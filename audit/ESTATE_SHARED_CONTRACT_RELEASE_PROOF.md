# Estate shared-contract release Proof Packet

**State:** `VERIFIED_LOCAL` — exact-head protected CI and review pending

**Proof level:** `VERIFIED_LOCAL`

**Date:** 2026-07-28

**Repository:** `szl-holdings/platform`

**Pull request:** #540 (draft)

**Workcell / task ID:** `PLATFORM-540-ESTATE-CONTRACT-AUDITABILITY`

**Producing actor:** Codex, operating as BuildWarden and ProofSmith for
`stephenlutar2-hash`

## Objective

Publish a deterministic, independently recomputable identity for Platform-owned
design, API, generated-client, and shared-contract bytes without implying
registry publication, cross-repository adoption, or deployment.

The corrective objective is to ensure that:

1. verification cannot rewrite a stale manifest before checking it;
2. consumers receive the exact allowlisted roots needed to reject omitted or
   additional files;
3. Turbo cannot replay a cached manifest build or freshness test when an
   external allowlisted tree changes; and
4. the release evidence contains the fields and exact command results required
   for audit.

## Source identity

- protected base revision:
  `4fd8824f32d7eedc0cd341fd4f4d57722b9d45e9`;
- cache-correction parent revision:
  `eceecb560d8dc92c72dfe47aa1d4f8bbb8af47d1`;
- baseline audit revision:
  `ace0f748819ad44190d5946c2f5909daef0b29f3`;
- tracked-input correction revision tested in the review addendum:
  `4e62ce4a926df9193d5b622915d298f9b634c933`;
- this packet is a proof-only follow-up to the tested implementation revision;
  the immutable final PR head must be taken from GitHub review metadata.

The corrected implementation produced release ID
`sha256:9af5861b8849ccc11813717187a15e52f644d64e97970b9cf0143eea47abf493`
over six components, 782 files, and 17,297,465 input bytes. The manifest
document is 167,960 bytes.

## Scope

The release closes the exact source bytes for:

- the Platform design system;
- the repository-owned Forge type contract re-exported by the design system;
- the OpenAPI contract and generator configuration;
- the present React and Zod generated clients, including their public
  entrypoint, runtime, and re-export boundaries; and
- shared TypeScript contracts.

Each component now serializes its allowlisted `inputs`. The builder enumerates
only Git-tracked regular files below those roots, so ignored editor artifacts
and other untracked local files cannot enter a release identity. Consumers must
reject missing or additional tracked files before recomputing file,
component-tree, and release digests.

The exported-dependency closure is limited to repository-owned source.
Third-party packages are not vendored or claimed as released here.

`STRUCTURE_ONLY` means the release binds generated bytes that are present. It
does not prove those bytes were freshly derived from the included OpenAPI
source and generator configuration.

## Plan summary

1. Preserve the reviewed six-component closure from the closed predecessor.
2. Add a non-mutating manifest verification command and regression.
3. Serialize component input roots and test the published enumeration contract.
4. Disable Turbo cache for both manifest build and freshness test tasks.
5. Regenerate the manifest intentionally, then verify it without mutation.
6. Record exact commands, numeric exits, output summaries, and truth limits.
7. Restrict release enumeration to Git-tracked regular files and prove ignored
   local artifacts cannot affect the identity.

## Patch summary

- `packages/estate-contract-release/src/build.mjs`
  - publishes each component's allowlisted `inputs`;
  - derives each inventory from `git ls-files`;
  - rejects missing, empty, symbolic-link, and non-regular tracked inputs.
- `packages/estate-contract-release/package.json`
  - adds `verify`, which invokes `build.mjs --check` and does not write.
- `packages/estate-contract-release/src/build.test.mjs`
  - proves verification preserves the manifest bytes;
  - proves all component input roots are serialized;
  - proves ignored and untracked files do not enter the manifest;
  - proves both relevant Turbo tasks have `cache: false`.
- `packages/estate-contract-release/README.md`
  - documents verify-before-build ordering and consumer enumeration.
- `packages/estate-contract-release/manifest.json`
  - intentionally regenerated with the published input roots.
- `turbo.json`
  - disables cache for both package-specific build and test tasks.

No UI, route, database, deployment, registry, or external consumer was changed.
Screenshot evidence is therefore not applicable.

## Baseline observed commands

All commands below were run from the repository root against exact
implementation revision
`ace0f748819ad44190d5946c2f5909daef0b29f3`.

| Command | Exit | Observed output summary |
|---|---:|---|
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe packages/estate-contract-release/src/build.mjs --check` | 0 | `PASS sha256:887345...c944`, six components |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check packages/estate-contract-release/src/build.mjs` | 0 | no syntax errors |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check packages/estate-contract-release/src/build.test.mjs` | 0 | no syntax errors |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --test packages/estate-contract-release/src/build.test.mjs` | 0 | focused package tests passed locally; canonical `platform_tests` remains `UNAVAILABLE` |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --test lib/api-spec/scripts/codegen.test.mjs` | 0 | focused API codegen tests passed locally; canonical `platform_tests` remains `UNAVAILABLE` |
| `.\node_modules\.bin\turbo.cmd run build --filter=@szl-holdings/estate-contract-release --dry-run=json` | 0 | package build resolved `cache=false`, local cache false, remote cache false |
| `.\node_modules\.bin\biome.cmd check turbo.json packages/estate-contract-release/package.json packages/estate-contract-release/src/build.mjs packages/estate-contract-release/src/build.test.mjs` | 0 | four files checked, no fixes |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe scripts/qa/scan-secrets.js` | 0 | `CLEAN — no secrets found.` |
| `git diff --check 4fd8824f32d7eedc0cd341fd4f4d57722b9d45e9...ace0f748819ad44190d5946c2f5909daef0b29f3` | 0 | no whitespace errors |
| `git status --porcelain=v1` | 0 | empty output at the tested implementation revision |

The baseline manifest was intentionally regenerated before that exact-revision
check. All subsequent baseline verification used the non-mutating `--check`
path.

## Review-correction observed commands

These commands were run from the repository root against exact correction
revision `4e62ce4a926df9193d5b622915d298f9b634c933`.

| Command | Exit | Observed output summary |
|---|---:|---|
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe packages/estate-contract-release/src/build.mjs --check` | 0 | `PASS sha256:9af586...f493`, six components |
| `C:\Users\steph\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --test packages/estate-contract-release/src/build.test.mjs` | 0 | 13 tests passed, including ignored/untracked-file exclusion |
| `git diff --check 4fd8824f32d7eedc0cd341fd4f4d57722b9d45e9...4e62ce4a926df9193d5b622915d298f9b634c933` | 0 | no whitespace errors |

## Verification notes

- The non-mutating regression reads the manifest bytes before and after
  `build.mjs --check` and requires byte-for-byte equality.
- The published input-root regression compares every manifest component with
  its canonical component definition.
- The tracked-input regression adds an ignored `.DS_Store` below an allowlisted
  root and requires byte-for-byte manifest equality.
- The external-tree mutation regression changes actual copied Forge and Zod
  source bytes and requires new component and release digests.
- Turbo 2.9.18 dry-run evidence reports both local and remote cache disabled for
  `@szl-holdings/estate-contract-release#build`.
- The local checkout's dependency links were incomplete for direct
  design-system and shared-contract TypeScript checks; those attempts exited 2
  with module-resolution errors. They are not presented as source failures or
  as passing evidence. Exact-head hosted Typecheck remains a merge gate.

## Public-claim determination

- source bytes: `VERIFIED_LOCAL`;
- generated output lineage: `STRUCTURE_ONLY`;
- registry publication: `UNAVAILABLE`;
- cross-repository adoption: `UNAVAILABLE`;
- production deployment: `UNAVAILABLE`.

No public capability, customer, revenue, compliance, registry, adoption, or
deployment claim is added. Hash closure proves only the bytes and consumer
rules represented by the manifest.

## Security determination

- no credential, token, environment file, authentication path, or network
  boundary was added;
- the exact tested implementation revision returned `CLEAN` from the repository
  secret scanner;
- exact-head hosted security checks remain pending and are not inferred from
  local results.

## Known gaps and merge gate

- exact-head protected CI is pending;
- exact-head independent review is pending;
- generated-client derivation remains `STRUCTURE_ONLY`;
- registry publication, cross-repository adoption, and deployment remain
  `UNAVAILABLE`;
- local targeted TypeScript verification is unavailable in this checkout
  because required dependency links are incomplete.

Keep the PR draft until the exact final head has terminal required checks and no
unresolved review findings. Do not treat this packet, a green check, or the
manifest hash as publication or deployment proof.

## Consumer rule

A consumer must pin an immutable protected Platform Git revision, download the
manifest from that revision, enumerate every file beneath each component's
published `inputs`, reject missing or additional files, recompute every file,
component-tree, and release digest, and record the accepted `release_id` in its
own source-bound evidence. Mutable `main` references fail the contract.
