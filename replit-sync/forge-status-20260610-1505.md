# FORGE STATUS — locked-8 alignment PRs MERGED + HF synced — 2026-06-10 ~15:05 EDT
**Perplexity (parent) → Forge · T-6 to Warhacker**

> This CLEARS the stale caveat in `forge-workorder-20260610-afternoon.md` §1.A that said
> "do not flip the live served count to 8 until the founder Lean-runner confirms."
> **That confirmation already happened.** Lean PR #219 is MERGED, `locked_count_eight`
> (by `decide`, no axioms) is on lutar-lean@main, and the served surfaces are now correctly 8.
> **The served count IS 8 — that is the correct, verified state. Do NOT revert it to 5.**

## WHAT PARENT MERGED + SYNCED THIS SESSION (all clean, verified)
Your `chore/align-locked-8` PRs were reviewed (safety-scanned: 0 codename re-intro, 0 bare
SLSA/cert over-claim, 0 locked-5 regression, 0 trust=100%) and **MERGED**:
- **a11oy #299** → merged `50c5c887` (STATUS.md, console/index.html, corpus/lean README)
- **killinchu #98** → merged `e34ae3f8` (Dockerfile, a11oy_code_engine.py, cathedral.html, killinchu_elite_console.py)
- **platform #329** → merged `c18445b2` (replit-sync canonical docs)
- **platform #330** (this work-order doc) → merged `60dcd2cf`
- (developers #5, docs-site #20, .github #149, pitch-collateral #2 were already closed/merged.)

**SHARED-MODULE LOCKSTEP (important):** killinchu #98 changed `a11oy_code_engine.py` (a SHARED module).
Parent applied the **identical** byte change to a11oy's copy in lockstep (a11oy commit `00866cf7`) so the
drift guard stays green. **`a11oy_code_engine.py` is now byte-identical across ALL 4 locations**
(GH a11oy / GH killinchu / HF a11oy / HF killinchu = blob `9daceb3a`). If you edit any shared module,
apply the SAME edit to BOTH apps in the same window.

**HF SYNC (parent owns HF mirrors):** all merged served files pushed byte-identical to the HF Spaces —
HF a11oy commit `8783eb3d` (STATUS, console, corpus README, a11oy_code_engine) + HF killinchu commit
`fbbedc94` (Dockerfile, a11oy_code_engine, cathedral, elite console). Both Spaces factory-restarted.
Drift guards GREEN (HF Space module-drift guard + Shared-source drift guard = success).

## CURRENT VERIFIED LIVE STATE (don't redo)
- locked-proven = **8** {F1,F4,F7,F11,F12,F18,F19,F22} @ c7c0ba17 — consistent on a11oy /console,
  killinchu /elite, HF org card, GitHub org profile, anatomy, a-11-oy.com cathedral.html source, web/operator.html.
- Operator widget **Chaski** live on every served HTML surface in both apps (vendored 0-CDN, live-wired to
  /api/a11oy/code/chat|agent + v4 ledger). Byte-identical `static-vendor/a11oy-operator-widget.{js,css}` in both apps.
- 3 HF Spaces RUNNING; a-11-oy.com HTTP 200.

## YOUR NEXT OPERATIONAL ITEMS (to make the remaining stubs real)
1. **a-11-oy.com Hetzner redeploy** — GitHub main `cathedral.html` is locked-8, but the box (167.233.50.75)
   still serves the old 5-set. If you have box access, run once as root:
   `curl -fsSL <raw a11oy>/ops/install-a11oy-autodeploy.sh | sudo bash` (3-min poll → `a11oy-rebuild`).
   Otherwise this stays founder-gated (parent cannot SSH).
2. **Self-hosted brain (flips Chaski stub→live, NO provider token)** — stand up Qwen2.5-Coder-32B-AWQ on the
   RTX box (ollama/vLLM), expose via Cloudflare Tunnel → `https://brain.a-11-oy.com` with OUR OWN bearer, set
   Space secret `SZL_LOCAL_LLM_URL` (+ `SZL_LOCAL_LLM_KEY`) on SZLHOLDINGS/{a11oy,killinchu}, and update the
   `szl-local` `model_slug` placeholder → `qwen2.5-coder:32b` so routing receipts stay truthful. The agent
   flips to live the instant the secret is set. (See `team/SELF_HOSTED_BRAIN.md`.)
3. **szl-uds-deployment PRs** — rebase onto main (post-#73 grype fix): **#57** (verify receipt signing,
   Ed25519 verifier) + **#51** (cosign SLSA L2 bundle — keep wording L1-honest / L2-build-attested,
   L3 roadmap). After rebase + green CI, parent merges the clean ones. Signing-key infra stays no-self-merge.
4. **Real-time tab wiring** — continue per `UNIFICATION_CAPABILITY_TAB_MAP.md` + `LIVE_SOURCES_VERIFIED.md`:
   server-side jittered polls only (FastAPI httpx 10-15s), honest cached/stale labels, 0 client CDN,
   verified sources only (NVD/CISA KEV/Yahoo/Coinbase/GitHub/HF/Hatun-MCP). Never mock a PAUSED service.
5. **Wave24 Lean** (branch, no self-merge): admissibility-certification soundness (conditional, axiom-clean);
   connect Wave23 BFT to the signed execution certificate; first honest Semantic Linearizability property.

## DOCTRINE — CURRENT HARD GATE (locked=8)
locked-proven = EXACTLY 8 {F1,F4,F7,F11,F12,F18,F19,F22} @ c7c0ba17 (`locked_count_eight`, no-axiom);
Λ=Conjecture 1; Khipu BFT=Conjecture 2; SLSA L1 honest · L2 build-attested · L3 roadmap (never bare
L3/FedRAMP/IronBank/CMMC/ATO); no user-visible codenames; trust never 100%; 0 runtime CDN; no fabricated
data; killinchu effector simulated; GitHub↔HF byte-identical on shared modules; never commit a key;
never weaken a gate; no Lean self-merge.

## HANDSHAKE
After each push, list changed served files in `SYNC_STATUS.md` so parent mirrors to HF byte-identical +
factory-restart. Never both edit the same file in one window. Real live data mandatory. Report to
`replit-sync/forge-report-<date>.md`.
