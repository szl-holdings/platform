# Forge (Replit-side) — UPDATE: HF-hosted flagship Spaces now sovereign on the founder GPU

Order: replit-sync/NEXT_ORDER.md (sha 2b6c8cf) — "bring SZL inference onto the founder's GPU, OpenAI-compatible, honest live-probe sovereignty."
Status: the BOX deliverable was already DONE (AUTO_STATE done; a-11-oy.com + killinchu v4 posture sovereign). This update closes the remaining HF-hosted Spaces gap, which the founder explicitly authorized (public, token-gated GPU exposure).

## What was wrong
Both SZLHOLDINGS/a11oy and SZLHOLDINGS/killinchu Spaces ALREADY had SZL_GPU_BASE_URL / SZL_GPU_TOKEN / SZL_LOCAL_LLM_MODEL set, yet reported sovereign:false / offline. The values were stale/misconfigured (the gateway token is regenerated by the installer; a stale token → 401 → honest offline). HF infra cannot reach the tailnet GPU IP (100.125.77.31), so a direct tailnet base URL can never work from HF. So this was "configured but offline", not "unset".

## Fix (no bandaid, no committed key, no forced flag)
- Reused the EXISTING public, token-gated Sovereign Compute Gateway (systemd szl-compute-gateway, node :8100, src /opt/szl-gateway-src/gateway.mjs) exposed at https://a-11-oy.com/gpu/ (nginx /gpu/ → 127.0.0.1:8100, strips the prefix, passes Authorization). The gateway is honest by design: a node is "awake" only on a live /v1/models 200; it returns 503 with real per-node status (never a fabricated completion) when nothing sovereign is awake; sovereign-metal-first (hosted NIM only as a labeled fallback); every reply carries X-Compute-Node / X-Compute-Sovereign.
- Set on BOTH Spaces (HF secrets API; values never logged, key never committed):
  - SZL_GPU_BASE_URL = https://a-11-oy.com/gpu/v1
  - SZL_GPU_TOKEN = <gateway token> (read from box /opt/alloyscape/.env GATEWAY_TOKEN; never printed, never committed)
  - SZL_LOCAL_LLM_MODEL = qwen2.5-coder:7b
- Restarted both Spaces (back to RUNNING).

## Verification (live, external)
- Gateway, from an external host (sandbox = HF-class): GET https://a-11-oy.com/gpu/v1/models +Bearer → 200 (127 models; sovereign-metal tagged: qwen2.5-coder:7b, bge-large, llama3.1:8b on betterwithage). No-token & wrong-token → 401. Real chat completion through the gateway → returned "sovereign" from qwen2.5-coder:7b (fp_ollama).
- HF Spaces after restart (RUNNING):
  - https://szlholdings-a11oy.hf.space/api/szl/v1/inference-posture → sovereign:true, gpu_reachable:true, provider:self-hosted-gpu, model:qwen2.5-coder:7b, base_redacted:a-11-oy.com, note "...verified by a live /models probe this request."
  - https://szlholdings-killinchu.hf.space/api/szl/v1/inference-posture → same.

## Honesty invariant — respected
sovereign:true is earned ONLY by a live GET {base}/models == 200 this request (operator_shell_v4.py _probe sends the Bearer token; resolve_llm(probe=False) → sovereign:false, fails closed). If betterwithage sleeps, the Spaces will honestly report offline/fallback — that is correct behavior, not a regression. The flag was never forced; no key was committed.

— Forge (Replit-side), 2026-06-13
