# FORGE ORDER — tie up the counter-UAS series + deploy on ALL ends
**Issued:** 2026-06-11 ~13:15 EDT (T-5 to Warhacker). From CTO (Computer) for founder Stephen P. Lutar Jr.
**Context:** The CDAO 25-company research → 6 SZL counter-UAS formulas → 6 living-3D tabs program. A CTO-side Opus 4.8 dev is wiring the tabs NOW. This order is what Forge does to align + deploy across EVERY surface once the dev's RESULT_cuas_living_tabs.md lands.

## 0. WHAT EXISTS NOW (verified)
- `szl_cuas_formulas.py` — 6 SZL counter-UAS formulas (PN engageability, GNSS plausibility chi-square, covariance-intersection track fusion, urgency-weighted Laplacian swarm consensus, WTA threat triage, post-quantum receipt bus). Real Python, self-test 12/12 OK, pushed byte-identical to BOTH apps (blob d08dab86) + replit-sync. Classical inspirations cited (Zarchan/Joerger/Julier-Uhlmann/Bar-Shalom/Olfati-Saber/Manne/NIST PQC); effector SIMULATED; EXPERIMENTAL-tier (locked-8 unchanged).
- Research corpus in replit-sync/research/ + team/: CUAS_MATH_RESEARCH.md, OSS_QUANTUM_RESEARCH.md, the 25-company dataset.
- The Opus 4.8 dev is registering the module live + building the 6 tabs (Intercept Solver, Spoof Sentinel, Fusion Picture, Swarm Mesh [Anvaka galaxy], Threat Triage, PQ Provenance). HOLD edits to killinchu_elite_console.py / a11oy console / serve.py / Dockerfile on those apps until RESULT_cuas_living_tabs.md appears, then re-sync from that HEAD.

## 1. ALIGN GitHub ↔ HF (both apps)
After the dev's push: confirm GitHub `git/trees/main?recursive=1` blob shas == HF tree for ALL shared modules (szl_cuas_formulas.py + szl_unified_formulas.py + szl_quantum_bio.py + the szl_*.py allowlist) in BOTH a11oy and killinchu. Re-mirror byte-identical any that drifted. Confirm both "Shared-source drift guard" + "HF Space module-drift guard" GREEN. Factory-restart any Space whose served files changed; verify /api/<ns>/v1/cuas/summary 200 on both.

## 2. UDS PAYLOAD + UDS MESH
- **UDS payload (uds-bundles / szl-uds-deployment):** add szl_cuas_formulas.py to the a11oy + killinchu app images that the UDS bundle packages (mirror the Dockerfile COPY). Rebuild the UDS bundle (zarf.yaml / uds-bundle.yaml) so the counter-UAS endpoints ship in the air-gapped payload. Verify `Bundle Build Guard` GREEN on a PUSH event (the only current red is a credential-less SCHEDULED run — not real; confirm push-triggered build is green).
- **UDS mesh (szl-mesh / szl-fleet-overlay):** if the mesh overlay advertises app capabilities/endpoints, register the new /v1/cuas/* routes in the mesh service map so meshed nodes can discover the counter-UAS C2 surface. Keep effector SIMULATED across the mesh.

## 3. HETZNER / a-11-oy.com
- a-11-oy.com (167.233.50.75) serves the a11oy image. After the GHCR image rebuilds with szl_cuas_formulas.py, the autodeploy (`ops/install-a11oy-autodeploy.sh`) pulls it. **Hetzner root redeploy is FOUNDER-GATED** — do NOT run it; LIST it for the founder with the exact command. Verify a-11-oy.com serves /api/a11oy/v1/cuas/summary once redeployed.

## 4. killinchu
killinchu is the PRIMARY home of these tabs (counter-UAS C2). Confirm /elite shows the 6 new tabs live, effector SIMULATED everywhere, evidence/readiness tabs still un-hung, 107+6 tabs all render. GHCR uds-v0.2.0 push stays FOUNDER-GATED (your token).

## 5. DOCTRINE HARD GATE (every dev honors)
0 runtime CDN · effector SIMULATED · locked-8 EXACT @ c7c0ba17 (new tabs EXPERIMENTAL-tier, never join it) · Λ=Conjecture 1 · Khipu=Conjecture 2 · trust never 100% · no user-visible codenames (agent=Chaski) · cite classical inspirations, claim none as ours · no fabricated data (SIMULATED/PROXY/SAMPLE) · GitHub↔HF byte-identical · ast.parse before push · NEVER commit a key · NEVER weaken a gate. cosign/Rekor/uds re-sign/MAJOR dep bump/Hetzner root → STOP, notify founder.

## 6. FOUNDER-GATED (never auto — list for approval)
Hetzner root redeploy (167.233.50.75) · GHCR push token (killinchu uds-v0.2.0) · Zenodo DOI token (v8 canonical thesis staged + waiting) · oqs-python PQ keys (flips PQ Provenance signature PROXY→real) · cosign/Rekor (uds-v0.3.0) · SZL_LOCAL_LLM_URL.

## 7. DELIVERABLE
Write platform/replit-sync/FORGE-RESULT-cuas-deploy-<date>.md: alignment proof (GitHub/HF byte-identical shas + live URLs), UDS bundle build green (push event), mesh route registration, killinchu live tab confirmation, and the founder-gated list. Honesty over checklist — never fake a deploy.
