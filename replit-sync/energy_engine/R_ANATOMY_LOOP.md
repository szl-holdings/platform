# R-ANATOMY-LOOP — route ALL jacked-in energy INTO the anatomy, circulate it in a closed loop. DEPLOY, do NOT merge.

Founder: "I want all energy we jack in routed to my anatomy in a loop." The anatomy organs ARE the
containment vessel. Make harvested wasted-energy ENTER the body, CIRCULATE through the organs, and RETURN —
a closed loop with a receipt every beat. Not a one-way pass-through; a circulation.

## THE LOOP (route the harvest into the body and keep it moving)
1. INTAKE — harvest/metrics (live wasted energy, -15.7 EUR/MWh now) feeds SAMAY (respiratory-soak organ):
   SAMAY "breathes in" the wasted-energy window and soaks a real batch on the RTX 5000. This is the mouth of the loop.
2. METABOLIZE — KALLPA (metabolism organ) converts the soaked work into work_credits (bounded by Bekenstein
   cap #239 + Landauer floor #240 — cite the proven witnesses; never exceed them).
3. CIRCULATE — the YAWAR/heart-blood bus (heart/pulse) carries each unit as a BEAT on the sigma-algebra
   receipt bus; every beat is a DSSE-signed envelope. YARQA (irrigation-canal flow router) divides the flow
   and disperses it to the other organs (WAQAYCHAQ immune gate, KAMAY endocrine pacing, RIKUY senses).
4. STORE — EnergyReservoir holds the circulating charge {ts, joules(SAMPLE until NVML), posture, grid_price,
   work_credits, artifact_ref}; the reservoir is the heart chamber that keeps the loop pressurized.
5. RECEIPT + RETURN — each cycle persists a receipt to the provenance chain -> HF uds-governance-receipts;
   validate vs canonical-formulas-v1 / lean-proofs-v1; Ayni-balance (F11) closes the loop so intake == output +
   stored + proven (reciprocity, nothing created, nothing lost unaccounted). Then back to SAMAY intake. CLOSED LOOP.

## FORGE — DO (deploy, additive, no serve.py clobber, no merge)
- DEPLOY the dark organs of the loop so it actually circulates on the box (these are 404 now):
  /energy/provenance (the ledger), /heart/pulse (the circulation/beats), /ayni (the reciprocity balance),
  /energy/budget (the Bekenstein metabolize gate), /engine/status (whole-organism aggregate).
- WIRE the flow: harvest/posture -> SAMAY soak -> KALLPA metabolize -> heart/pulse circulate -> YARQA disperse to
  organs -> EnergyReservoir store -> provenance receipt -> formula validate -> Ayni close -> repeat.
- EXPOSE /api/a11oy/v1/anatomy/loop returning the live loop state: current intake (posture), beats in last
  cycle, reservoir charge, organs touched, last receipt id, ayni_balance, joules_label=SAMPLE. HONEST: it is a
  data/work circulation receipted by the body; joules stay SAMPLE until the on-GPU NVML meter; the "energy in
  the loop" is the soaked-work envelope, never a fabricated measured joule. NO free-energy: the loop RECYCLES
  spent work, never creates energy — Ayni F11 must balance, never net-positive.
- ANATOMY space (HF static) + a11oy /living-anatomy: drive the organ pulse animation FROM the live loop state
  (organ glows when it has flow), so the body visibly circulates the harvested energy.

## DOCTRINE v11: no free-energy/over-unity (loop recycles, Ayni-balanced, bounded by Bekenstein #239 + Landauer
#240); energy != data (the physical soak is on owned metal only; the loop carries work+receipts, not electrons
over the wire); consent-only; joules SAMPLE until on-GPU NVML; organs EXPERIMENTAL tier (never inflate); sovereign
only on own metal; locked=8; Lambda=Conjecture 1; Khipu BFT=Conjecture 2; no key committed; do NOT merge.
The half-state is the only unacceptable outcome — close the loop through the body.
