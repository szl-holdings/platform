# R-CHASKI-ONLINE-ONELOOP — wake chaski, route it as 2nd lung into the ONE unified loop. DEPLOY, do NOT merge.

Founder: "chaski should be on — make sure it's handled and routed properly; you choose the loop and handle it."
VERIFIED NOW: /compute-pool shows chaski (100.76.58.50, tailnet-gpu) reachable=FALSE, sovereign=FALSE — its
Ollama is NOT answering. It is registered HONESTLY (never fabricated as up). betterwithage is the live sovereign
GPU (reachable=true). To bring chaski on, its Ollama must answer on the tailnet — that last step is on the box.

## LOOP DECISION (LOCKED by the agent, per founder "you choose the loop"): ONE UNIFIED LOOP.
The single canonical loop = the anatomy circulation loop /anatomy/loop (PR #341 + #343 YARQA-as-circulatory).
NOT two loops. One loop -> one receipt chain -> one Ayni balance -> no double-counting, no schema drift. Both
sovereign GPUs are DUAL INTAKE LUNGS feeding the SAME loop: betterwithage = primary lung; chaski = second lung
(added capacity + redundancy). If chaski drops, the loop keeps breathing on betterwithage (reactive never starves).
If both up, the loop circulates across both. This is the handled, chosen architecture.

## FORGE — DO
1. WAKE CHASKI (the box step): on the chaski machine (100.76.58.50), bring Ollama up and bind it tailnet-
   reachable:  set OLLAMA_HOST=0.0.0.0:11434 ; start `ollama serve` (or the service) ; confirm `ollama list`.
   Verify from the box / fabric:  curl http://100.76.58.50:11434/v1/models -> 200. If this needs founder hands on
   that machine, say so explicitly in the report with the exact commands (do NOT fabricate it as up before Ollama
   answers — keep reachable=false until it truly responds).
2. ROUTE IT PROPERLY (the moment Ollama answers): /compute-pool flips chaski reachable=true + gpu_nodes_reachable
   1->2 (honest, driven by a real probe, not a flag). Register chaski as the SECOND SAMAY intake lung in
   szl_anatomy_loop so the ONE loop circulates across both GPUs. Route inference/soak by an honest policy:
   prefer betterwithage (primary), spill/parallelize to chaski when it's up and a window is open; energy-gate both
   to cheap/negative-price windows. If chaski runs an exporter (Windows exporter.ps1 or Linux nvidia-smi push),
   its joules accrue into the SAME meter/ledger (measured), else SAMPLE for that node honestly.
3. FAILOVER: the loop and the marketplace must degrade cleanly if either GPU drops — never stall, never starve a
   reactive turn, never fabricate a node as up. Surface both lungs honestly in /anatomy/loop + /compute-pool +
   the HF energy Space (chaski node lights up only on a real 200).

## DOCTRINE v11 (hard): chaski registers HONESTLY (reachable only on a real probe 200, never a flag); ONE loop,
one receipt chain, one Ayni balance (no double-count); joules MEASURED only via a real exporter per node, SAMPLE
otherwise; sovereign:true ONLY on own metal (betterwithage + chaski both own metal); no free-energy (#239/#240,
Ayni-balanced); energy != data; reactive never starves; locked=8; Λ=Conjecture 1; Khipu=Conjecture 2; no key/seed
committed; do NOT merge. Wake chaski, route it as the 2nd lung of the one loop, fail over honestly. Report the
exact founder command if the box step needs hands on the chaski machine.
