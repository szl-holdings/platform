# Amaru vertical — "Provenance & Trust Anchor" (5 tabs in a11oy)

## WHAT AMARU IS (from Replit/Forge side, founder-confirmed)
Amaru = PROVENANCE ANCHOR: public-ledger anchoring + post-quantum (PQC) hardening + (with Rosie) fleet-wide proof management. It is the cryptographic backbone that makes every other vertical's signed receipts court-admissible and future-proof. Part of the 5-module mesh (amaru, a11oy, sentra, rosie, killinchu).

## DOCTRINE NAME RULE (hard)
'amaru' is a BANNED user-visible codename. In the UI the vertical is named **"Provenance & Trust Anchor"** (the honest role). NEVER show 'amaru' to users. The capability gets 5 tabs; the codename stays remapped. (Internal module refs may keep the name in backend/comments only.)

## THE 5 TABS (each unique, live, beautiful, governed + signed)
1. **Public-Ledger Anchor (LIVE)** — anchor receipt Merkle roots to a public ledger; show live anchor events, confirmations, a tamper-evident chain. Live signal: real public timestamping/transparency-log style (e.g. show real chain heights/timestamps from a public source; label any sample). Governed anchor action -> signed receipt.
2. **Post-Quantum Signing (PQC)** — show PQC posture: ML-DSA/Dilithium + ML-KEM/Kyber readiness, hybrid classical+PQC signatures on receipts. Honest maturity (what's live vs roadmap). Sign a demo receipt with the hybrid scheme, verify it.
3. **Receipt Provenance Graph (3D)** — the full provenance DAG across ALL verticals: decision -> Λ-receipt -> anchor -> theorem -> DOI. anvaka/vasturiano 3d-force-graph, zoomToFit framed, click node -> Palantir-style detail panel. This is the cross-vertical 'one chain governs everything' visual (pairs with the unified ledger).
4. **Tamper / Audit Verifier** — paste/replay any receipt -> verify signature + anchor + chain integrity; flip-a-byte tamper test that CATCHES the break. Court-admissibility framing.
5. **Anchor Health** — Boss-Tech-style observability of the anchoring fabric: anchor latency, confirmation depth, PQC coverage %, quorum (ties to UDS 4/4), live counters auto-poll.

## LIVE/REAL SOURCES (free/public; premium = connect-ready, never fake)
- Public transparency-log / timestamp style data for anchor confirmations (use a real reachable public source; label sample if needed).
- Reuse a11oy's REAL DSSE/Khipu receipt machinery + szl-lake for the provenance DAG (this is genuinely ours and live).
- PQC: represent real algorithm posture (NIST FIPS 203/204/205 — ML-KEM/ML-DSA/SLH-DSA) honestly; sign with available libs in-image; label what's live vs roadmap. 0 CDN.

## INTEGRATION
- Sits in the CORE/governance band of a11oy (it's the backbone), accessible as its own 5-tab vertical.
- Powers the 'court-admissible provenance' claim that de-risks every other vertical (RealEstate/Finance/Legal/Enterprise/Warhacker).
- Rosie (Operator organ, already ingested) = receipt orchestration; Amaru = anchoring + PQC. They pair: Rosie orchestrates, Amaru anchors+hardens.

## HONESTY: locked=8 {F1,F4,F7,F11,F12,F18,F19,F22}; Λ=Conjecture 1; PQC posture labeled live-vs-roadmap honestly; no fabricated anchors; SAMPLE labeled; 0 CDN; no user-visible 'amaru'.
