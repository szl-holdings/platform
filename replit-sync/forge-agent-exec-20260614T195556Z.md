# Forge executor pass — 2026-06-14T19:55:56Z

**Executor:** /usr/local/sbin/forge-agent (real sovereign-LLM executor)
**Provider:** sovereign-gpu http://100.125.77.31:11434 :: qwen2.5-coder:7b
**Actionable items:** 19 | **Founder-gated skipped:** 0
**Doctrine:** v11 — no fabricated operational flags, no secrets committed, no keystone self-merge.

---

## Understanding
The order requires rebuilding and redeploying the `a11oy` container on Hetzner at IP 167.233.50.75 to the latest commit from GitHub main (d333b997 or newer). Additionally, it mandates proving that specific URLs return the correct HTTP codes and content types by curling them from the box's public hostname. The order also includes merging specific PRs into GitHub main before redeploying the container.

## Plan
1. **Merge PRs into GitHub Main:**
   - Merge #397 energy/06-holographic.
   - Merge #394 energy/01-operator, #391 energy/02-ledger, #393 energy/03-projection, #392 energy/04-dashboard.

2. **Rebuild and Redeploy the `a11oy` Container:**
   - Pull the latest code from GitHub main.
   - Build the container image.
   - Push the new image to Hetzner.
   - Restart the `a11oy` and `killinchu.a11oy.net` containers.

3. **Prove URLs:**
   - Curl each URL and record the HTTP codes and content types.
   - Verify the sovereign probe state.

4. **Confirm Restart Recovery:**
   - Simulate a container restart.
   - Confirm both apps auto-recover within 60 seconds.

## Decisions / Analysis
1. **Merge PRs:**
   - Merge #397 energy/06-holographic to ensure the `szl_holo3d.js` and other 3D graphs are available.
   - Merge #394, #391, #393, and #392 to complete the press-play operator stack.

2. **Rebuild and Redeploy:**
   - Ensure the new container image is byte-identical to GitHub main.
   - Verify that the `a11oy` and `killinchu.a11oy.net` containers are running correctly after redeployment.

3. **Prove URLs:**
   - Use `curl` commands to verify each URL's HTTP code and content type.
   - Ensure the sovereign probe confirms `sovereign:true` if Tailscale 5050 is live, otherwise confirm `honest sovereign:false`.

4. **Confirm Restart Recovery:**
   - Simulate a container restart using `systemctl restart <service-name>`.
   - Check logs to ensure both apps recover within the specified time frame.

## Status
1. **Merge PRs:**
   - #397 energy/06-holographic: RECOMMENDED (merge pending CI green).
   - #394, #391, #393, #392 energy/01-operator, #391 energy/02-ledger, #393 energy/03-projection, #392 energy/04-dashboard: RECOMMENDED (merge pending CI green).

2. **Rebuild and Redeploy:**
   - Container rebuild/redeploy: BLOCKED (awaiting PR merges to complete).

3. **Prove URLs:**
   - https://a11oy.net/static/shared/szl_holo3d.js: BLOCKED (container redeploy pending).
   - https://a11oy.net/api/a11oy/v1/restraint/info: BLOCKED (container redeploy pending).
   - https://a11oy.net/api/a11oy/v1/restraint/bench: BLOCKED (container redeploy pending).
   - https://a11oy.net/estate-hologram: BLOCKED (container redeploy pending).
   - https://a11oy.net/nemo, /autoreview, /factory, /constitution, /quant, /grc, /restraint: BLOCKED (container redeploy pending).
   - https://a11oy.net/api/a11oy/v1/honest: BLOCKED (container redeploy pending).
   - https://killinchu.a11oy.net/elite, /elite/globe, /elite/restraint, /api/killinchu/v1/restraint/info: BLOCKED (container redeploy pending).
   - Sovereign probe: BLOCKED (container redeploy pending).

4. **Confirm Restart Recovery:**
   - Confirm restart recovery: BLOCKED (container redeploy pending).

## Founder-gated
- Merge PRs into GitHub main.
- Confirm Tailscale 5050 is live for sovereign probe.

---
_Generated by the sovereign fabric and committed by forge-agent. Irreversible/prod writes (merges, deploys, secrets) remain founder-gated by design._
