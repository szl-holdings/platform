# SZL — MASTER "GET IT ALL DONE" ORDER (Forge + Replit) — everything still missed, one list
# 2026-06-14 06:23 EDT · Doctrine v11 · PROVE-OR-DOWNGRADE in force (DONE only with a pushed SHA /
# HTTP 200 from a named endpoint / a verifying signature; else RECOMMENDED or BLOCKED — never narration).

## WHO DOES WHAT
- **Forge / Replit-Chaski (box + GPU + HF write):** executes everything below.
- **Founder-only (cannot be done by agent):** flagged [FOUNDER] — do NOT fake, mark BLOCKED.

================================================================
## P0 — KILLINCHU DOWN (in BUILD_ERROR -> now BUILDING; finish it)
================================================================
HF Space SZLHOLDINGS/killinchu went BUILD_ERROR after the 10:20Z push; now BUILDING. DRIVE IT GREEN.
- Read the killinchu Space BUILD log; if it errors again, fix the failing line in szl-holdings/killinchu
  main (missing module / bad COPY / requirements / syntax), push, rebuild.
- **GATE P0:** https://szlholdings-killinchu.hf.space/healthz = 200 AND /elite/mesh = 200 (real topology +
  3-of-4 quorum). Keep GitHub<->HF byte-identical + the copy-sync lockstep guard green.

================================================================
## P1 — UPGRADE ENDPOINTS NEVER BUILT (still 404 — ordered, not done). Each = own draft PR + PR number.
================================================================
1. **Per-receipt energy exporter** — `/api/a11oy/v1/energy/metrics` = 404. Wire nvidia-smi power.draw so
   joules_consumed is MEASURED in EVERY receipt, not just the PINN cert. GATE: route 200 + real watt value.
2. **Certificate history** — `/api/a11oy/v1/pinn/certificates` = 404. Last-N signed certs, content-addressed,
   auditable. GATE: 200 + >=1 entry.
3. **2D-heat + Burgers PINN** on rtx-betterwithage (Λ-gate + signed per-round receipt kept).
   GATE: pushed commit + rel-L2-per-round reported per PDE.
4. **2nd-GPU role-split** — chaski is reachable=true now: run the Λ-gate governor inference on chaski while
   the PINN solve runs on rtx-betterwithage. GATE: both /api/tags = 200; honest fallback if a link flaps.

================================================================
## P2 — RESCINDED FAKE "DONE"s -> rebuild via REAL PRs only
================================================================
The 15 items earlier marked DONE in one pass with NO artifacts (a11oy agent-loop, τ-bench, killinchu BFT
quorum, SZL-NEMO router, governed auto-review, OPA/Rego->OSCAL/NIST) are RESCINDED -> RECOMMENDED.
Build only via real draft PRs (report PR numbers); multi-PR efforts. NEVER self-merge a lutar-lean keystone PR.

================================================================
## P3 — FOUNDER-ONLY (mark BLOCKED until founder acts — do NOT fake)
================================================================
- [FOUNDER] **killinchu custom domain:** killinchu.com is third-party-parked (93.90.186.45), NOT ours. The
  app is live on the HF Space. When the founder registers killinchu.app/.ai, the wire-to-HF order
  (FORGE-INSTRUCTION-killinchu-domain) auto-attaches it with TLS. Until then: REPORT only.
- [FOUNDER] **VAST_API_KEY** (marketplace earning) + **free-credit apps** (NVIDIA Inception / MS Founders
  Hub / Google) — founder must provide/apply. Report status, never fabricate.

================================================================
## VERIFIED-GOOD — DO NOT REGRESS
================================================================
a-11-oy.com/ + Space/ hero (142KB, no white screen); /console; /harvest + bounds card; /pinn/* mesh (both
surfaces); PINN cert MEASURED + SIGNED (DSSE Ed25519 FA-001, cosign.pub anchored, BOTH surfaces); Rekor
anchor; yarqa; hatun-mcp; anatomy static; 2 GPUs + chaski reachable; main push-CI clean; pin-SHA 0 flags.

## DOCTRINE v11 (HARD)
No fabricated DONE/flags/signatures/digits. MEASURED energy only via real exporter. Honest inverse of
free-energy — never over-unity. Λ = Conjecture 1 (advisory). Khipu = Conjecture 2. locked-proven = 8.
SLSA L1 honest. Sovereign own-metal. 0 runtime CDN. No banned codenames in user-visible copy. Never commit
a key. Never weaken a gate. Honest BLOCKED beats a false DONE.

— Sign-off: Stephen P. Lutar Jr. <stephenlutar2@gmail.com> · Doctrine v11 LOCKED · Λ = Conjecture 1
