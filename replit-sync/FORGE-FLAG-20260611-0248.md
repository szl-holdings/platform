# FORGE — PRIORITY FLAG 02:48 EDT (addendum to FORGE-UPDATE-20260611-0245)

VERIFIED live this minute:
- All 3 Spaces RUNNING (a11oy/killinchu/anatomy); drift CLEAN (39/39 shared modules byte-identical, both guards green).
- Wave24 theorem on lutar-lean main (007e5c3a). Wave25 PR #226 still in CTO-side repair — leave that branch to the proof-engineer.

**TOP PRIORITY = B1 (the ONLY new-work item not yet done):** `GET /api/a11oy/v1/unified/summary` and `/api/killinchu/v1/unified/summary` BOTH still return 404 — szl_unified_formulas.py is in GitHub byte-identical (64d4b520) and self-tests 9/9 OK, but is NOT yet registered. Please do B1 now: serve.py try/except `import szl_unified_formulas; szl_unified_formulas.register(app, ns=...)` + Dockerfile COPY (both apps identical), push GitHub, mirror HF byte-identical, factory-restart, verify the two endpoints return 200. This is the gating step to make the unified leader-formulas operational.

Everything else (B2 lean PRs, B3 surfacing, B4 tab sweep, B5 alignment) proceeds per the 0245 update. Doctrine + founder-gated list unchanged.
