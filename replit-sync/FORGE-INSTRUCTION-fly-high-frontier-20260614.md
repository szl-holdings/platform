# SZL Forge — STANDING ORDER: FLY HIGH (frontier backlog, run after the current waves)

Runs after: PNT mesh (DONE, live), Holographic Ops, and the Upgrades wave. This is the always-on
"keep flying high" backlog so the estate keeps evolving. PROVE-OR-DOWNGRADE in force (DONE only with a
pushed commit SHA / HTTP 200 / verifying signature; else RECOMMENDED or BLOCKED). Doctrine v11, clean-room,
0 runtime CDN, sovereign own-metal, 2D fallback, MEASURED/MODELED labels, no fabrication.

## F1 — Unify the two physics pillars into ONE living surface
Both pillars are now live: COMPUTE bounds (PINN cert, MEASURED+SIGNED) and SENSING limits (PNT, MODELED).
Co-locate physics_bounds with the PNT files so /api/a11oy/v1/pnt/limits shows compute_bounds wired:true
(currently honest "not wired yet"). One fundamental-limits index, shared constants, one /verify over both.
GATE: /pnt/limits -> both pillars wired:true; one signed unified verify.

## F2 — Elevate every flagship to the holographic standard
After the Holographic Ops tab ships, give killinchu, yarqa, hatun-mcp, anatomy each a governed 3D hero
view bound to their real signal (no fake geometry, 2D fallback, MEASURED/MODELED labels). Search the
leaders in each vertical, take the best, make it ours, clean-room. GATE: each flagship renders a live 3D
hero + degrades to 2D; values match its API.

## F3 — Make the estate self-describing
A single /api/a11oy/v1/estate index that lists every live capability across the estate (compute, energy,
PNT, governance, the flagships) with honest status + MEASURED/MODELED label per surface, feeding both
/anatomy/loop and the Holographic Ops hero. GATE: /estate 200, every entry reflects a real probe.

## F4 — Push the GPU fabric harder (when the founder's rig joins)
When the RTX-4000 rig appears on the tailnet, run the heavy PNT + PINN sweeps split across all 3 sovereign
GPUs (rtx-betterwithage + chaski + rig), publish MEASURED energy receipts per node, and show the 3-node
split live in the hex-heightmap. GATE: 3 GPU columns live; per-node MEASURED joules. (Rig IP = founder action.)

## F5 — Continuous honesty hardening
Keep the overclaim/doctrine/lockstep/lean-citation guards green across all repos. Any new surface must ship
with its MEASURED/MODELED labels and a test. Never let a number go unlabelled. RECOMMENDED ongoing.

## P3 [FOUNDER] (report BLOCKED, never fake)
RTX-4000 rig tailnet IP · VAST_API_KEY (marketplace earning) · killinchu domain (.app/.ai) · free-credit apps.

## DOCTRINE v11 (HARD)
No fabricated DONE/flags/numbers/geometry/signatures. MEASURED only via real run/exporter. Λ = Conjecture 1
(advisory, never proven trust). Honest inverse of free-energy. Clean-room, cite-never-plagiarize. 0 CDN,
system fonts, sovereign. 2D fallback mandatory. Never commit a key. Never merge a lutar-lean keystone PR.
Honest BLOCKED beats a false DONE. Keep flying high — but never fly on a fabricated number.

— Sign-off: Stephen P. Lutar Jr. <stephenlutar2@gmail.com> · Doctrine v11 LOCKED · Λ = Conjecture 1
