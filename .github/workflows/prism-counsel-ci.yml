# ARCHIVED: PRISM Counsel CI/CD
#
# PRISM Counsel is deprecated (task #579). This workflow is disabled and will not
# run automatically. Azure infrastructure targets have been decommissioned.
#
# See: artifacts/prism-counsel/DEPRECATED.md
# See: ops/frontier/disposition-matrix.md

name: PRISM Counsel CI/CD (ARCHIVED)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is archived and will not run automatically."
        required: false

permissions:
  contents: read

jobs:
  archived-notice:
    name: Workflow Archived
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::warning::This workflow is archived. PRISM Counsel (task #579) is deprecated."
          echo "See artifacts/prism-counsel/DEPRECATED.md and ops/frontier/disposition-matrix.md"
