name: Truth drift

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '17 6 * * *'
  workflow_dispatch:
    inputs:
      full_claim_scan:
        description: Scan the full tracked claim corpus, including legacy debt
        required: false
        default: false
        type: boolean
      require_surface_freshness:
        description: Require the public surface observation to be no older than seven days
        required: false
        default: false
        type: boolean
      require_truth_freshness:
        description: Require the generated truth snapshot to be no older than seven days
        required: false
        default: false
        type: boolean
      refresh_truth:
        description: Regenerate source-of-truth evidence and stage a protected refresh package
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  truth-drift:
    name: truth-drift
    if: >-
      github.event_name != 'workflow_dispatch' || inputs.refresh_truth == false
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10.26.1

      - name: Install Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify deterministic public surface manifest
        run: pnpm exec tsx tools/truth/generate-public-surfaces.ts --check

      - name: Verify live public surface evidence
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: pnpm surfaces:check

      - name: Require a current public surface observation
        if: >-
          github.event_name == 'schedule' ||
          (github.event_name == 'workflow_dispatch' && inputs.require_surface_freshness)
        run: pnpm surfaces:freshness

      - name: Verify deterministic local truth
        run: pnpm truth:generate -- --verify-local

      - name: Verify live remote truth
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: pnpm truth:generate -- --verify-local --verify-remote

      - name: Validate evidence labels and structure
        run: pnpm claims:validate

      - name: Require a current generated truth snapshot
        if: >-
          github.event_name == 'schedule' ||
          (github.event_name == 'workflow_dispatch' && inputs.require_truth_freshness)
        run: pnpm truth:freshness

      - name: Reject hardcoded metric drift
        run: pnpm claims:drift

      - name: Reject newly introduced claim drift
        if: github.event_name == 'pull_request' || github.event_name == 'push'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: node tools/truth/check-claims.ts --base "$BASE_SHA"

      - name: Audit the complete claim corpus
        if: github.event_name == 'workflow_dispatch' && inputs.full_claim_scan
        run: node tools/truth/check-claims.ts --all

      - name: Reject source-of-truth drift
        run: git diff --exit-code -- artifacts/SOURCE_OF_TRUTH.json artifacts/PUBLIC_SURFACES.json

  refresh-truth:
    name: generate current evidence and stage protected refresh
    if: >-
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && inputs.refresh_truth)
    concurrency:
      group: truth-evidence-refresh-${{ github.repository }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      issues: write
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out exact protected source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Bind generation to exact current protected main
        id: source
        env:
          EXPECTED_SHA: ${{ github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$GITHUB_REF" != 'refs/heads/main' ]; then
            echo "::error::Truth refresh must run from refs/heads/main, not ${GITHUB_REF}."
            exit 1
          fi
          observed_sha="$(git rev-parse HEAD)"
          current_main="$(
            gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/main" \
              --jq '.object.sha'
          )"
          if [ "$observed_sha" != "$EXPECTED_SHA" ] || [ "$current_main" != "$EXPECTED_SHA" ]; then
            echo "::error::Protected main moved or checkout identity diverged;" \
              'rerun from the new main.'
            exit 1
          fi
          echo "sha=${observed_sha}" >> "$GITHUB_OUTPUT"

      - name: Determine whether evidence refresh is due
        id: due
        env:
          EVENT_NAME: ${{ github.event_name }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import datetime as dt
          import json
          import os
          from pathlib import Path

          due = os.environ.get('EVENT_NAME') == 'workflow_dispatch'
          reason = 'owner dispatch requested an immediate refresh'
          if not due:
              path = Path('artifacts/SOURCE_OF_TRUTH.json')
              try:
                  payload = json.loads(path.read_text(encoding='utf-8'))
                  stamp = str(payload['generated_at']).replace('Z', '+00:00')
                  generated = dt.datetime.fromisoformat(stamp).astimezone(dt.timezone.utc)
                  now = dt.datetime.now(dt.timezone.utc)
                  age = now - generated
                  if generated > now:
                      due = True
                      reason = 'canonical evidence timestamp is future-dated'
                  else:
                      due = age >= dt.timedelta(days=5)
                      reason = f'canonical evidence age is {age.total_seconds() / 86400:.2f} days'
              except (OSError, KeyError, TypeError, ValueError, json.JSONDecodeError) as error:
                  due = True
                  reason = f'canonical evidence could not be dated safely: {type(error).__name__}'
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f"value={'true' if due else 'false'}\n")
              output.write(f"reason={reason}\n")
          print(f"refresh_due={due}: {reason}")
          PY

      - name: Record bounded no-op decision
        if: steps.due.outputs.value != 'true'
        run: echo "No refresh PR is required because ${{ steps.due.outputs.reason }}."

      - name: Install pnpm
        if: steps.due.outputs.value == 'true'
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10.26.1

      - name: Install Node.js
        if: steps.due.outputs.value == 'true'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install locked dependencies
        if: steps.due.outputs.value == 'true'
        run: pnpm install --frozen-lockfile

      - name: Regenerate current local and remote evidence
        if: steps.due.outputs.value == 'true'
        run: |
          set -euo pipefail
          pnpm truth:generate
          pnpm truth:generate -- --verify-local
          pnpm claims:validate
          git diff --check

      - name: Detect evidence change
        if: steps.due.outputs.value == 'true'
        id: changed
        shell: bash
        run: |
          if git diff --quiet -- artifacts/SOURCE_OF_TRUTH.json; then
            echo 'value=false' >> "$GITHUB_OUTPUT"
            echo 'The generated evidence is already current.'
          else
            echo 'value=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Bind package to unchanged protected main
        if: steps.changed.outputs.value == 'true'
        id: package
        env:
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          observed_sha="$(git rev-parse HEAD)"
          current_main="$(
            gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/main" \
              --jq '.object.sha'
          )"
          if [ "$observed_sha" != "$SOURCE_SHA" ] || [ "$current_main" != "$SOURCE_SHA" ]; then
            echo "::error::Protected main advanced while the evidence package was generated; rerun."
            exit 1
          fi
          package_sha256="$(sha256sum artifacts/SOURCE_OF_TRUTH.json | awk '{print $1}')"
          echo "sha256=${package_sha256}" >> "$GITHUB_OUTPUT"

      - name: Retain the exact generated refresh package
        if: steps.changed.outputs.value == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: source-of-truth-refresh-${{ github.run_id }}
          path: artifacts/SOURCE_OF_TRUTH.json
          if-no-files-found: error
          retention-days: 7

      - name: Open or update the protected refresh work item
        if: steps.changed.outputs.value == 'true'
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
          PACKAGE_SHA256: ${{ steps.package.outputs.sha256 }}
        run: |
          set -euo pipefail
          title='chore(truth): promote current source-of-truth evidence'
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          body="$(cat <<EOF
          A current evidence package was generated from protected main.

          - source revision: \`${SOURCE_SHA}\`
          - workflow run and seven-day artifact: ${run_url}
          - generated path: \`artifacts/SOURCE_OF_TRUTH.json\`
          - package SHA-256: \`${PACKAGE_SHA256}\`

          Download the artifact, verify its bytes, and promote it through a signed,
          DCO-complete pull request. The automation intentionally does not push a
          branch or create a pull request with \`GITHUB_TOKEN\`, because token-authored
          pull-request events do not trigger the protected check suite.
          EOF
          )"
          number_output="$(
            gh issue list \
              --repo "$GITHUB_REPOSITORY" \
              --state open \
              --limit 1000 \
              --search "${title} in:title" \
              --json number,title \
              --jq ".[] | select(.title == \"${title}\") | .number"
          )"
          mapfile -t numbers < <(printf '%s' "$number_output")
          if [ "${#numbers[@]}" -gt 1 ]; then
            echo "::error::Multiple open protected refresh work items have the exact title."
            exit 1
          elif [ "${#numbers[@]}" -eq 1 ]; then
            gh issue comment "${numbers[0]}" --repo "$GITHUB_REPOSITORY" --body "$body"
          else
            gh issue create --repo "$GITHUB_REPOSITORY" --title "$title" --body "$body"
          fi
