# .trivyignore - strict Trivy scanner configuration for szl-holdings/platform.
#
# Keep this file intentionally empty of advisory identifiers. Findings remain
# visible in SARIF, while the unsuppressed Grype HIGH/CRITICAL job is the release
# gate. Dependencies must be upgraded, replaced, or removed instead of ignored.
