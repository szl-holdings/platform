REPLIT MASTER PAYLOAD — SZL PRODUCTION PLATFORM (FULL SYSTEM)

MISSION
Build a production-ready SZL platform with:
- Terra → Real estate + NYC/NYS distress intelligence
- Firestorm → Enterprise cyber defense platform (defensive, authorized)
- Core → Unified command layer
- Website → public-facing product
- Real data ingestion pipelines
- Enterprise security (auth, RBAC, audit)
- Cloud-ready deployment structure

==================================================
SECTION 1 — ARCHITECTURE (PRODUCTION)
==================================================

CREATE MONOREPO:

/apps
  /terra
  /firestorm
  /core-ui
  /website

/services
  /api
  /ingestion
  /auth
  /audit
  /ai

/packages
  /types
  /design-system
  /utils

/infra
  /docker
  /migrations
  /env

STACK:
Frontend → React + Vite + TypeScript
Backend API → Node (Express)
DB → PostgreSQL
Cache → Redis (optional future)
Auth → JWT (upgrade path to Auth0/Azure AD)
Deployment → Docker-ready

==================================================
SECTION 2 — DATABASE (EXPANDED)
==================================================

CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  email TEXT UNIQUE,
  password_hash TEXT,
  role TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE distress_properties (
  id SERIAL PRIMARY KEY,
  address TEXT,
  borough TEXT,
  county TEXT,
  type TEXT,
  stage TEXT,
  source TEXT,
  filing_date TIMESTAMP,
  score INT
);

CREATE TABLE audit_logs (
  id SERIAL PRIMARY KEY,
  user_id INT,
  action TEXT,
  entity TEXT,
  timestamp TIMESTAMP DEFAULT NOW()
);

==================================================
SECTION 3 — AUTH + RBAC (ENTERPRISE BASE)
==================================================

BUILD AUTH SERVICE:

/services/auth

FEATURES:
- JWT login
- hashed passwords (bcrypt)
- roles:
  - admin
  - broker
  - agent
  - investor
  - security_analyst

MIDDLEWARE:
- requireAuth
- requireRole

==================================================
SECTION 4 — API SERVICE (PRODUCTION)
==================================================

/services/api/src/index.ts

ROUTES:

// AUTH
POST /api/auth/login
POST /api/auth/register

// TERRA
GET /api/terra/distress
GET /api/terra/deals

// FIRESTORM
GET /api/firestorm/vulnerabilities
GET /api/firestorm/incidents

// AUDIT
GET /api/audit/logs

==================================================
SECTION 5 — NYC / NYS DATA INGESTION (REALISTIC)
==================================================

CREATE SERVICE:
/services/ingestion

THIS IS CRITICAL — NO FAKE SCRAPING

IMPLEMENT:

1. FILE-BASED INGESTION
- CSV upload endpoint
- parse NYC property / lien / foreclosure datasets

2. SCHEDULED INGESTION
- cron job (node-cron)
- pull from:
  - NYC Open Data (ACRIS datasets)
  - NY State public datasets
  - county records (manual ingestion pipeline)

3. NORMALIZATION
- clean address
- map borough / county
- dedupe properties
- classify distress type

4. STORE INTO:
distress_properties

==================================================
SECTION 6 — DISTRESS ENGINE (PRODUCTION)
==================================================

API:

GET /api/terra/distress?borough=Bronx&type=foreclosure

FEATURES:
- filtering
- pagination
- sorting

ADD ALERT LOGIC:
- new records trigger alert table (future expansion)

==================================================
SECTION 7 — FIRESTORM (REALISTIC SECURITY BASE)
==================================================

IMPORTANT:
THIS IS DEFENSIVE + SIMULATION ONLY

BUILD:

- vulnerability ingestion (manual or scan import)
- incident tracking
- anomaly logs (simulated for now)

NO:
- real hacking
- external targeting
- exploit tooling

==================================================
SECTION 8 — AUDIT SYSTEM
==================================================

LOG:

- login
- data access
- API calls
- admin actions

STORE IN:
audit_logs

==================================================
SECTION 9 — FRONTEND APPS
==================================================

TERRA UI:
- distress table
- filters
- investor view

FIRESTORM UI:
- vulnerabilities list
- incidents list

CORE UI:
- summary dashboard

WEBSITE:
- landing page
- Terra + Firestorm sections

==================================================
SECTION 10 — WEBSITE (REAL POSITIONING)
==================================================

HEADLINE:
SZL — Command Systems for Real-World Operations

SECTIONS:
- Terra (find + close deals)
- Firestorm (defend systems)
- CTA

==================================================
SECTION 11 — DEPLOYMENT (REAL)
==================================================

DOCKERIZE:

- api service
- postgres
- frontend builds

CREATE:
docker-compose.prod.yml

PREP FOR:
- Azure App Service
- AWS ECS
- Railway / Render (fast start)

==================================================
SECTION 12 — ENV CONFIG
==================================================

.env:

DATABASE_URL=postgres://szl:szl@db:5432/szl
JWT_SECRET=supersecret

==================================================
SECTION 13 — SECURITY BASELINE
==================================================

ENFORCE:

- password hashing
- JWT validation
- input validation
- no open endpoints
- audit logging

==================================================
SECTION 14 — SEED DATA (UPGRADED)
==================================================

ADD:

- multiple NYC borough properties
- mixed distress types
- sample vulnerabilities
- sample incidents

==================================================
SECTION 15 — SUCCESS CRITERIA
==================================================

SYSTEM IS VALID IF:

- auth works
- API secured
- distress data loads
- ingestion works
- UI connects to real API
- audit logs populate

==================================================
FINAL INTENT
==================================================

SZL becomes:

- Terra → deal discovery + execution platform
- Firestorm → security validation platform
- Core → observability layer

THIS IS A REAL FOUNDATION.

NOT A DEMO.
NOT A TOY.

TARGET:
“This is infrastructure we can deploy.”